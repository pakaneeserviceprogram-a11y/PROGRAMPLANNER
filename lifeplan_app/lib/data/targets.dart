/// Shared numeric goals used to compute progress across screens. Later these
/// should become per-user, editable settings (see DATA_MODEL.md — SavingGoal,
/// SalesGoal, ExerciseWeeklyGoal); for now they're fixed constants so every
/// screen's percentages agree with each other.
const kSavingTarget = 50000.0;
const kSalesTarget = 250000.0;
const kExerciseWeeklyTarget = 5;
