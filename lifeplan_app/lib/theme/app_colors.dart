import 'package:flutter/material.dart';

/// Design tokens shared across the app. Mirrors the color system used in the
/// UI mockups (see the design canvas / DATA_MODEL.md at the project root).
class AppColors {
  AppColors._();

  static const bg = Color(0xFFFAF9F6);
  static const surface = Color(0xFFFFFFFF);
  static const surface2 = Color(0xFFF4F3F0);

  static const text = Color(0xFF211F30);
  static const textMuted = Color(0xFF6B6878);
  static const textFaint = Color(0xFF9C99A6);

  static const border = Color(0xFFE8E6E1);

  static const primary = Color(0xFF5B57E8);
  static const primaryDark = Color(0xFF8B3FD9);

  static const exercise = Color(0xFF3FAE6B);
  static const exerciseSoft = Color(0xFFE3F5EA);

  static const nutrition = Color(0xFFD9506B);
  static const nutritionSoft = Color(0xFFFBE4EA);

  static const sleep = Color(0xFF3F4A73);
  static const sleepSoft = Color(0xFFE7EAF3);

  static const work = Color(0xFF4C6FEF);
  static const workSoft = Color(0xFFE7ECFE);

  static const crm = Color(0xFFF2874B);
  static const crmSoft = Color(0xFFFDEBE0);

  static const finance = Color(0xFF2E8B9E);
  static const financeSoft = Color(0xFFE1F0F2);

  static const learning = Color(0xFF8B4FE0);
  static const learningSoft = Color(0xFFF0E6FB);
}
