# 16/09/2026 — ปฏิทินรายเดือน/รายปี + คัดลอกกิจกรรมประจำไปวันอื่น

บันทึกรอบงานวันที่ 16 ก.ย. 2026 บนเครื่อง `D:\WORK\PROGRAMPLANNER` — เวอร์ชันแอป **1.4.0+5**
ใช้ไฟล์นี้อ่านต่อเมื่อไปทำงานต่อที่เครื่องอื่น (ภาพรวมโปรเจกต์อยู่ที่ `README.md`, วิธีทำงานต่ออยู่ที่ `HANDOFF.md`)

---

## 1. สิ่งที่ผู้ใช้ขอ

> เพิ่มปฏิทินรายเดือน และรายปี และทำตัวคัดลอกกิจกรรมที่ทำบ่อย ๆ ไปไว้วันอื่น และแก้ไขวันอื่นได้
> เช่น ทำทุกวันจันทร์ อังคาร / นอนทุกวันกี่โมง / วิ่งตอนเช้าทุก 6 โมง

## 2. ทำอะไรไปบ้าง

| สิ่งที่ผู้ใช้ทำได้ | วิธีใช้ในแอป |
|---|---|
| ดูปฏิทินรายเดือน | ตารางเวลา → ปุ่ม **เดือน** — จุดสีใต้วันที่ = หมวดของกิจกรรมวันนั้น, แตะวันที่เพื่อดูรายการด้านล่าง, ‹ › เลื่อนเดือน, แตะชื่อเดือนเพื่อไปมุมมองปี |
| ดูปฏิทินทั้งปี | ปุ่ม **ปี** — 12 เดือนย่อ ตัวเลขมุมขวา = จำนวนกิจกรรมทั้งเดือน แตะเดือนเพื่อเปิดดู |
| เลื่อนสัปดาห์ | มุมมองวัน/สัปดาห์มี ‹ › และปุ่ม **วันนี้** |
| เพิ่มกิจกรรมหลายวันในครั้งเดียว | ปุ่ม + → เลือกวัน จ อ พ พฤ ศ ส อา ได้หลายวัน หรือกดปุ่มลัด **ทุกวัน / จ–ศ / ส–อา** (เช่น "วิ่งตอนเช้า" 06:00 ทุกวัน) |
| คัดลอกกิจกรรมเดิมไปวันอื่น | แตะกิจกรรม → หัวข้อ "คัดลอกไปวันอื่นด้วย" เลือกวัน → บันทึก |
| แก้ไขเฉพาะวันนั้น | แตะกิจกรรมของวันนั้นแล้วแก้ตามปกติ (สำเนาแต่ละวันแยกกัน) |
| แก้ไขทุกวันพร้อมกัน | แตะกิจกรรม → เปิดสวิตช์ **ใช้การแก้ไขกับทุกวันในชุดนี้** (เช่น เปลี่ยนเวลาเข้านอนจาก 22:30 เป็น 23:00 ทุกวัน) |
| ลบ | ปุ่ม "ลบกิจกรรมนี้" → ถ้ามีหลายวันเลือก **ลบเฉพาะวันนี้** หรือ **ลบทุกวัน** / ปัดซ้ายลบเฉพาะวันนั้น |
| คัดลอกตารางทั้งวัน | มุมมองวัน → **คัดลอกทั้งวัน** (หรือไอคอนคัดลอกใต้ปฏิทินรายเดือน) เช่น ตารางวันจันทร์ → อังคาร–ศุกร์ |

- การคัดลอกจะ **ข้ามวันที่มีกิจกรรมชื่อและเวลาเดียวกันอยู่แล้ว** จึงกดซ้ำได้ไม่สร้างรายการซ้ำ
- การแจ้งเตือน (ป๊อปอัป+เสียง) ใช้ได้กับทุกวันที่คัดลอกไปทันที

## 3. โครงสร้างข้อมูล

`ScheduleEvent` เพิ่มฟิลด์ `seriesId` (String?)

- สำเนาแต่ละวันคือ **เรคคอร์ดแยก** (id ไม่ซ้ำ) → แก้ทีละวันได้ และระบบแจ้งเตือนเดิมใช้ได้โดยไม่ต้องแก้
- ผูกเป็นชุดด้วย `seriesKey = seriesId ?? id` — ต้นฉบับไม่ต้องมี `seriesId` สำเนาได้ `seriesId` = id ต้นฉบับ
- ข้อมูลเก่า/ไฟล์สำรองเก่าอ่านได้เหมือนเดิม (`backupFormatVersion` ยังเป็น 1)

## 4. ไฟล์

### ไฟล์ใหม่
```
lib/data/calendar_utils.dart        ← monthGrid, startOfWeek, addDays, addMonths, ชื่อเดือนไทย/ปี พ.ศ.
test/calendar_utils_test.dart       ← 4 เคส (ตารางเดือน 28/35/42 ช่อง, ข้ามเดือน/ปี, ป้ายภาษาไทย)
```

### ไฟล์เดิมที่แก้
- `lib/models/schedule_event.dart` — `seriesId`, `seriesKey`, `copyWith`
- `lib/data/repositories/schedule_repository.dart` — `seriesOf`, `copyToWeekdays`, `copyDay`, `updateSeries`, `deleteSeries`
- `lib/screens/schedule_screen.dart` — มุมมอง วัน/สัปดาห์/เดือน/ปี, ตัวเลือกหลายวัน, คัดลอก, แก้ไข/ลบทั้งชุด
  - แก้บั๊กเดิม: มุมมองสัปดาห์เลื่อนแนวตั้งไม่ได้ (ล้นจอเมื่อกิจกรรมเยอะ)
- `test/repository_test.dart` (+4), `test/schedule_screen_test.dart` (+5)
- `pubspec.yaml` — 1.3.0+4 → **1.4.0+5**
- `README.md`, `HANDOFF.md`, `DATA_MODEL.md`

## 5. ทดสอบ

- `flutter analyze` — ไม่มีปัญหา
- `flutter test` — ผ่าน **99 tests** (เดิม 86)
- ตรวจ layout ที่ขนาดจอมือถือ 360×740 ทั้ง 4 มุมมอง + ฟอร์ม + หน้าคัดลอกทั้งวัน ด้วย test ชั่วคราว — ไม่มี overflow

## 6. ข้อจำกัด / งานต่อ

- ตารางยังเป็น **ประจำสัปดาห์** — ในปฏิทินรายเดือน วันจันทร์ทุกสัปดาห์จึงแสดงกิจกรรมเหมือนกัน
  ถ้าต้องการนัดหมายเฉพาะวันที่ (เช่น นัดลูกค้า 25 ก.ย. ครั้งเดียว) ต้องเพิ่มฟิลด์ `date` + ปรับการแจ้งเตือนให้เป็นแบบครั้งเดียว
- APK รอบนี้ยังเซ็นด้วย debug key (เครื่องนี้ไม่มี `lifeplan-release.jks` / `key.properties`)
