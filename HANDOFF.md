# LifePlan — Handoff / เอกสารต่องาน

อัปเดตล่าสุด: 2026-09-19 (เป้าหมายโภชนาการเฉพาะบุคคล) — ใช้ไฟล์นี้เพื่อกลับมาทำงานต่อได้เร็ว ไม่ต้องไล่อ่านทั้งบทสนทนาใหม่

ดูภาพรวมโปรเจกต์/ฟีเจอร์ที่ `README.md`, โครงสร้างข้อมูลที่ `DATA_MODEL.md` — ไฟล์นี้เน้นเฉพาะ "จะทำต่อยังไง"

---

## 0. สรุปงานรอบล่าสุด

### รอบ 2026-09-19 (ต่อ) — เป้าหมายโภชนาการเฉพาะบุคคล

หน้าใหม่ `lib/screens/nutrition_plan_screen.dart` เข้าจากการ์ดในหน้าโภชนาการ

- **`lib/models/body_profile.dart`** — เพศ / **ปีเกิด** (เก็บปีเกิดแทนอายุ อายุจะขยับเองทุกปี) / ส่วนสูง / น้ำหนัก / ระดับกิจกรรม / เป้าหมายน้ำหนัก
  - เก็บใน box ใหม่ `body_profile` → เพิ่มใน `HiveBoxes.init()` และ **`BackupService._boxNames`** (อยู่ในไฟล์สำรอง) แต่ **ไม่อยู่ใน `recordBoxNames`** เพราะเป็นข้อมูลตั้งค่า ไม่ใช่ประวัติ (ล้างประวัติแล้วไม่หาย)
- **`lib/data/nutrition_plan.dart`** — ฟังก์ชันล้วน คำนวณ:
  - BMR ด้วย **Mifflin-St Jeor**, TDEE = BMR × ตัวคูณระดับกิจกรรม, ปรับ −15% (ลด) / +10% (เพิ่ม)
  - โปรตีน 1.2–2.0 ก./กก. ตามระดับกิจกรรม, ไขมัน 25% ของพลังงาน, น้ำตาลไม่เกิน 10% (WHO), ที่เหลือเป็นคาร์บ (clamp กันติดลบเมื่อเป้าพลังงานต่ำมาก)
  - **น้ำ 35 มล./กก. + 500 มล. ต่อการออกกำลังกาย 30 นาที/วัน** โดยดึงนาทีจริงจาก `ExerciseRepository` (เฉพาะที่ติ๊กว่าทำแล้ว)
  - BMI แปลผลตาม **เกณฑ์เอเชีย** (ท้วมที่ 23 ไม่ใช่ 25) + คำแนะนำที่อ้างอิงตัวเลขของผู้ใช้เอง
  - ข้อมูลไม่ครบ/ค่าติดลบ → คืน `null` (หน้าจอชวนให้กรอกแทนโชว์ตัวเลขมั่ว)
- ปุ่ม "ใช้ค่านี้เป็นเป้าหมายของแอป" เขียนทับเฉพาะเป้าโภชนาการใน `GoalSettings` (ไม่ยุ่งกับเป้าเงิน/ขาย/นอน) → ทุกหน้าที่ใช้เป้าหมาย rebuild เอง
- **บั๊กที่เทสต์จับได้**: พิมพ์ในช่องกรอกแล้วผลคำนวณไม่อัปเดต เพราะไม่ได้ `addListener` ให้ controller (บั๊กแบบเดียวกับช่องค้นเมนูไทย — การพิมพ์ใน TextField ไม่ทำให้ widget แม่ rebuild)
- **ข้อควรจำ (test)**: เทสต์ที่แตะไฟล์สำรอง/หน้าโภชนาการต้องเปิด box `body_profile` ด้วย (แก้ `backup_test` แล้ว)
- test ใหม่ `test/nutrition_plan_test.dart` (14 tests) — รวม **250 tests**

### รอบ 2026-09-19 (ต่อ) — keystore ใหม่ + เตือนสำรองข้อมูล

**1. keystore สำหรับเซ็น release (สำคัญมาก)**

- เครื่องนี้ไม่เคยมี keystore เลย ทุก build จึงเซ็นด้วย debug key = อัปเดตทับไม่ได้ ต้องถอนแล้วลงใหม่ (ข้อมูลหาย) ทุกครั้ง
- สร้างใหม่แล้วที่ `android/lifeplan-release.jks` + `android/key.properties` (**ทั้งคู่อยู่นอก git** ตาม `.gitignore` — ตรวจด้วย `git check-ignore -v` แล้ว)
  - alias `lifeplan`, RSA 2048, อายุ 10,000 วัน, SHA-256 `F6:ED:6A:23:...:0A:30` (ดูเต็มใน `android/README-signing.md`)
  - `android/app/build.gradle.kts` อ่าน `key.properties` อยู่แล้ว ไม่ต้องแก้ — ไม่มีไฟล์ก็ถอยไป debug key เหมือนเดิม
- **ถ้าไฟล์นี้หาย = อัปเดตแอปที่ลงไปแล้วไม่ได้อีกเลย** ต้องสำรองไฟล์ + รหัสผ่านไว้นอกเครื่อง
- แอปที่ลงด้วย debug key ก่อนหน้านี้ต้องถอนแล้วลงใหม่ **ครั้งเดียว** จากนั้นอัปเดตทับได้ตลอด

**2. เตือนสำรองข้อมูล** (`lib/data/backup_reminder.dart`)

- `AppSettings` เพิ่ม `backupReminderEnabled` (เปิดไว้), `backupReminderDays` (7), `lastBackupAt`
- ส่งออกไฟล์สำเร็จ → บันทึก `lastBackupAt` แล้วตั้งเตือนรอบใหม่
- **หน้าหลักขึ้นแบนเนอร์แดง**เมื่อเลยกำหนด (แตะแล้วไปหน้าสำรองข้อมูล) + **แจ้งเตือนของระบบ** ครั้งเดียวตอน 20:00 ของวันที่ครบกำหนด (id 7500)
- หน้าสำรองข้อมูลมีการ์ดบอกว่าสำรองล่าสุดกี่วันก่อน + สวิตช์ปิดการเตือน
- ผู้ใช้ใหม่ที่ยังไม่มีข้อมูลไม่โดนเตือน (`hasDataWorthBackingUp`)
- **ข้อควรจำ**: `hasDataWorthBackingUp` ต้องข้าม box ที่ยังไม่เปิด (`Hive.isBoxOpen`) ไม่งั้นหน้าหลักพังทั้งหน้าเวลามี box ไหนไม่ได้เปิด (เทสต์ dashboard จับได้)
- test ใหม่ `test/backup_reminder_test.dart` (9 tests) — รวม **236 tests**

**บั๊กที่เจอตอน build ตัวเซ็นจริงครั้งแรก**: R8 ล้มทั้ง build ด้วย `Missing class ...TextRecognizerOptions`
เพราะปลั๊กอิน `google_mlkit_text_recognition` อ้างถึงตัวอ่านภาษาจีน/ญี่ปุ่น/เกาหลี/เทวนาครี ซึ่งเป็น dependency แยกที่ไม่ได้ใส่
→ เพิ่ม `-dontwarn com.google.mlkit.vision.text.{chinese,devanagari,japanese,korean}.**` ใน `android/app/proguard-rules.pro`
(ถ้าวันหลังเพิ่มฟีเจอร์ ML Kit อื่นแล้ว build ล้มแบบเดียวกัน ให้ดูที่ไฟล์นี้ก่อน)

**APK ที่ส่งให้ผู้ใช้ (2026-09-19)**: เวอร์ชัน **1.7.0+8** arm64 32.2MB เซ็นด้วย keystore จริงแล้ว
(`CN=LifePlan`) — ขนาดโตจาก ~20MB เพราะ ML Kit; ถ้า build รวมทุก ABI จะได้ 50MB จึงควรใช้ `--split-per-abi` เสมอ

### รอบ 2026-09-19 (ต่อ) — สแกนนามบัตรเข้ารายชื่อลูกค้า

หน้าใหม่ `lib/screens/business_card_scan_screen.dart` เข้าจากการ์ดในหน้าค้นหาผู้มุ่งหวัง

- **`lib/data/business_card_scanner.dart`**
  - `MlKitCardTextRecognizer` ใช้ `google_mlkit_text_recognition` อ่านข้อความ **ในเครื่อง** ไม่ส่งรูปขึ้นคลาวด์ ไม่ต้องใช้ API key
  - **ML Kit ไม่รองรับภาษาไทย** (รองรับละติน/จีน/ญี่ปุ่น/เกาหลี/เทวนาครี) → ชื่อไทยบนนามบัตรอ่านไม่ออก แต่เบอร์โทร/อีเมล/เว็บ/ชื่อบริษัทอังกฤษอ่านได้ดี หน้าจอจึงเป็น "อ่านให้ก่อน แล้วให้ผู้ใช้ตรวจ/พิมพ์เพิ่ม" เสมอ
  - ถ้าวันหนึ่งต้องการไทยจริง ๆ: เปลี่ยน `CardTextRecognizer.instance` เป็น Tesseract (ออฟไลน์ ลงไฟล์ภาษาไทย) หรือ Google Cloud Vision (ต้องมี key + ค่าใช้จ่ายต่อรูป) — หน้าจอไม่ต้องแก้
  - `BusinessCardParser.parse()` เป็นฟังก์ชันล้วน แกะ ชื่อ/บริษัท/เบอร์/อีเมล/เว็บ ด้วย regex + คำใบ้ (`บริษัท`, `จำกัด`, `Co., Ltd`) และข้ามบรรทัดตำแหน่งงาน (`Sales Manager`, `ตัวแทน`) ไม่ให้กลายเป็นชื่อคน
  - เบอร์ถูกตัดสัญลักษณ์คั่นทิ้ง (`(081) 234-5678` → `0812345678`, `+66 81…` → `+66812345678`)
- `ClientSource.businessCard` เพิ่มในแหล่งที่มา — สแกนแล้วติดป้ายให้อัตโนมัติ และเว็บไซต์บนนามบัตรถูกเก็บเป็น `profileUrl`
- **ข้อควรจำ (test)**: ปุ่มบันทึกอยู่ท้ายหน้าใน `ListView` ต้อง `scrollUntilVisible(..., scrollable: find.byType(Scrollable).first)` ก่อนกด (หน้ามี Scrollable ของช่องกรอกข้อความซ้อนอยู่)
- test ใหม่ `test/business_card_test.dart` (13 tests) — รวม **227 tests**

### รอบ 2026-09-19 — นำเข้ารายชื่อจากสมุดโทรศัพท์ + แหล่งที่มาของลูกค้า

ต่อจากหน้าค้นหาผู้มุ่งหวัง — สองทางที่ "ได้รายชื่อจริงโดยไม่ต้องพึ่ง API ของโซเชียล"

- **`lib/data/contacts_import.dart`** — อ่านสมุดโทรศัพท์ผ่าน `flutter_contacts` (เพิ่ม `READ_CONTACTS` ใน AndroidManifest)
  - `ContactsSource` เป็น interface + `FakeContactsSource` → เทสต์หน้าจอได้โดยไม่ต้องมีเครื่องจริง (ปลั๊กอินรันใน unit test ไม่ได้)
  - **flutter_contacts 2.5 เปลี่ยน API จาก 1.x**: ใช้ `FlutterContacts.permissions.request(PermissionType.read)` และ `getAll(properties: {...})` (ไม่ใช่ `requestPermission()` / `getContacts()` แบบเดิม) และ `Contact.id`/`displayName` เป็น nullable
  - `suggest()` กันซ้ำด้วย **เบอร์โทร** เป็นหลัก (เทียบเฉพาะตัวเลข) เพราะชื่อในเครื่องมักเขียนไม่ตรงกับในแอป — ไม่มีเบอร์ถึงค่อยเทียบชื่อ
  - **บั๊กที่เทสต์จับได้**: ชื่อที่เป็นช่องว่างล้วนหลุดตัวกรอง เพราะเช็ค `isEmpty` โดยไม่ `trim()` ก่อน
  - **ข้อมูลไม่ออกนอกเครื่อง**: อ่านมาแสดงให้ติ๊กเลือก บันทึกเฉพาะคนที่เลือก คนที่ไม่ได้เลือกไม่ถูกเก็บที่ไหนเลย
- **`Client.source`** (`ClientSource` enum, ค่าเริ่มต้น `unknown`) — เลือกในฟอร์มแก้ไขลูกค้า, นำเข้าจากสมุดโทรศัพท์ติด `phonebook` ให้อัตโนมัติ
  - การ์ด "ลูกค้ามาจากไหน" ใต้รายชื่อลูกค้า สรุปจำนวนต่อแหล่ง + จำนวนที่ปิดการขายได้ (ต่อยอดรายงาน N = New Market)
- หน้าค้นหาผู้มุ่งหวังมีการ์ด "นำเข้าจากสมุดโทรศัพท์" เป็นทางเข้า
- test ใหม่ `test/contacts_import_test.dart` (13 tests) — รวม **214 tests**

### รอบ 2026-09-18 (ต่อ) — ค้นหาผู้มุ่งหวัง + ข้อมูลติดต่อลูกค้า

หน้าใหม่ `lib/screens/prospect_search_screen.dart` เข้าจากไอคอนแว่นขยายข้างหัวข้อ "ลูกค้าที่ต้องติดตาม"

- **`lib/data/contact_search.dart`** — ประกอบลิงก์หน้าค้นหาของแต่ละแหล่ง (แพตเทิร์นเดียวกับ `VideoSearch`)
  - **DBD ใช้ Google แบบจำกัดเว็บ** (`site:datawarehouse.dbd.go.th`) ไม่ยิงเข้าหน้าค้นหาภายในของ DBD เพราะรูปแบบ URL ภายในไม่ใช่ API สาธารณะ เปลี่ยนเมื่อไหร่ก็ได้ และหน้ารายละเอียดต้องล็อกอิน
  - **Facebook/Instagram เปิดหน้าค้นหาให้ผู้ใช้ค้นเองเท่านั้น** — Meta ปิด API ค้นหาผู้ใช้ตั้งแต่ปี 2018, IG API เข้าถึงได้เฉพาะบัญชีธุรกิจของตัวเอง, การ scrape ผิด ToS และเสี่ยง PDPA → **อย่าพยายามทำระบบดูดรายชื่ออัตโนมัติ** หน้าจอเขียนข้อจำกัดนี้ให้ผู้ใช้เห็นด้วย
  - `phoneUrl` ตัดอักขระที่ไม่ใช่ตัวเลข/+ ออก และสั้นกว่า 8 หลักถือว่าไม่ใช่เบอร์, `profileUrl` เติม `https://` ให้และต้องมีโดเมนจริง (มีจุด) ไม่งั้นข้อความธรรมดาจะกลายเป็นลิงก์มั่ว
- **`Client.phone` / `Client.profileUrl`** (String?, null = ยังไม่มี) — กรอกในฟอร์มแก้ไขลูกค้า แถวรายชื่อจะมีปุ่มโทรและปุ่มเปิดโปรไฟล์ (ปุ่มจางกดไม่ได้ถ้าค่าที่กรอกใช้ไม่ได้) เรคคอร์ดเก่าอ่านได้ตามปกติ
- หน้าค้นหามีปุ่ม "บันทึกชื่อนี้เข้ารายชื่อลูกค้า" สร้าง `Client` สถานะลูกค้าใหม่ทันที
- test ใหม่ `test/prospect_search_test.dart` (12 tests) — รวม **201 tests**

### รอบ 2026-09-18 (ต่อ) — ค้นคลิปความรู้บน YouTube (โมดูลเรียนรู้)

หน้าใหม่ `lib/screens/learning_videos_screen.dart` เข้าจากการ์ดในหน้าเรียนรู้

- **`lib/data/video_search.dart`** — ประกอบลิงก์ `youtube.com/results?search_query=...&sp=...`
  - **ทำไมไม่ดึงรายชื่อคลิป/ยอดวิวมาแสดงเอง**: ต้องใช้ YouTube Data API ซึ่งต้องสมัคร API key (Google Cloud) — ผู้ใช้เลือกแบบไม่ใช้ key ก่อน จึงส่ง "คำสั่งเรียงลำดับ" ไปให้ YouTube จัดอันดับให้แทน ได้ยอดวิวจริงโดยไม่มีโควตาจำกัด
  - `VideoSort` 4 แบบ (ยอดดู / ตรงคำค้น / ใหม่ล่าสุด / เรตติ้ง) → พารามิเตอร์ `sp` ของ YouTube เอง **ไม่ใช่ API สาธารณะ** ถ้า YouTube เปลี่ยนรูปแบบ ผลจะกลับไปเรียงปกติ ไม่ได้พังทั้งหน้า
  - `topics` = ชุดคำค้นสำเร็จรูป **11 หัวข้อ** ให้ตรงกับโมดูลในแอป (การเงิน/ออม/หุ้น/ลงทุน/ประกัน/สุขภาพ/ออกกำลังกาย/พัฒนาตนเอง) + ทักษะที่ผู้ใช้ขอเพิ่ม (การตลาด & การขาย, เรียนภาษา, บุคลิกภาพ & การพูด) เขียนคำค้นให้เจาะจงกว่าชื่อหัวข้อ และห้ามซ้ำกันข้ามหัวข้อ (มี test คุม)
- ใช้ `url_launcher` (เพิ่มใน pubspec รอบนี้) เปิดด้วย `LaunchMode.externalApplication` → เข้าแอป YouTube ถ้าเครื่องมี
- **จุดที่ทำให้เทสต์ได้**: `LearningVideosScreen(onOpenUrl:)` ให้เทสต์ดักว่าเปิด URL อะไร แทนการเรียก `launchUrl` จริง
- **ข้อควรจำ (test)**: หน้านี้มี `Scrollable` ซ้อน (ช่องกรอกข้อความมีของตัวเอง) — `scrollUntilVisible` ต้องระบุ `scrollable: find.byType(Scrollable).first`
- ถ้าจะทำต่อให้แสดงรายชื่อ+ยอดวิวในแอป: สมัคร API key แล้วเพิ่มตัวกลางเรียก Data API (แพตเทิร์นเดียวกับ `AuthService`) โดยหน้าจอเรียกผ่านตัวกลางแทน `searchUrl`
- test ใหม่ `test/video_search_test.dart` (9 tests) — รวม **189 tests**
- ทดสอบบน emulator แล้ว (ทั้งหัวข้อเดิมและหัวข้อที่เพิ่มรอบหลัง เช่น "แต่งตัวให้ดูดี มืออาชีพ"): แตะคำค้นแล้วระบบเปิดแอป YouTube จริง (`topResumedActivity=com.google.android.youtube`) แล้วส่งต่อให้ Chrome ซึ่งค้างที่หน้าต้อนรับครั้งแรกของ emulator เอง ไม่ใช่ปัญหาจากโค้ด

### รอบ 2026-09-18 (ต่อ) — ฐานข้อมูลเมนูอาหารไทย

- **`lib/data/thai_food_database.dart`** — เมนูไทยที่เจอบ่อย ~50 รายการ พร้อมพลังงาน/โปรตีน/แป้ง/ไขมัน/น้ำตาล/วิตามิน **ต่อหนึ่งหน่วยเสิร์ฟ**
  - **ค่าทั้งหมดเป็นค่าประมาณจากสูตรทั่วไป** ไม่ใช่ข้อมูลอ้างอิงที่ผ่านการตรวจสอบ — ฟอร์มเติมให้เป็นจุดตั้งต้นแล้วผู้ใช้แก้ต่อได้เสมอ (เขียนกำกับไว้ใต้ช่องค้นหาด้วย)
  - `search()` ตัดช่องว่างและตัวพิมพ์ทิ้ง ("ข้าว มันไก่" ก็เจอ) และดันชื่อที่ขึ้นต้นด้วยคำค้นขึ้นก่อน
  - เครื่องดื่มตั้งชื่อให้ตรงกับ `SleepNutritionInsights.caffeineKeywords` (กาแฟเย็น/ชาไทยเย็น/ชาเขียว) เลือกจากเมนูแล้วการ์ด "การนอนกับสิ่งที่กิน" จับได้เลย
- ฟอร์มบันทึกมื้ออาหารมีช่อง "ค้นเมนูไทย (ไม่บังคับ)" อยู่บนสุด เลือกแล้วเติมชื่อ + ตัวเลข + วิตามินให้ทั้งชุด
  - **บั๊กที่เทสต์จับได้**: `_FoodPicker` เดิม `setState` เฉพาะตอนผลลัพธ์เปลี่ยน ทำให้พิมพ์คำที่ไม่มีในฐานข้อมูลแล้วข้อความ "ไม่เจอเมนูนี้" ไม่ขึ้น (การพิมพ์ใน TextField ไม่ทำให้ widget แม่ rebuild เอง) — แก้เป็น setState ทุกครั้งที่ข้อความเปลี่ยน
- test ใหม่ `test/thai_food_test.dart` (7 tests) รวมการตรวจความสมเหตุสมผลของข้อมูล (น้ำตาลต้องไม่เกินแป้ง ชื่อไม่ซ้ำ ค่าไม่ติดลบ) — รวม **180 tests**
- ทดสอบบน emulator แล้ว: ช่องค้นหาอยู่บนสุดของฟอร์ม พิมพ์คำที่ไม่มีในฐานข้อมูลขึ้น "ไม่เจอเมนูนี้" ถูกต้อง (ยืนยันว่าบั๊ก rebuild แก้แล้วจริง)
  - **พิมพ์ไทยผ่าน adb ไม่ได้** (`adb shell input text` รองรับ ASCII และ emulator ไม่มี `cmd clipboard`) การค้นภาษาไทย + การเติมค่าอัตโนมัติจึงคุมด้วย widget test แทน

### รอบ 2026-09-18 (ต่อ) — จับคู่การนอนกับคาเฟอีน/มื้อดึก

การ์ด "การนอนกับสิ่งที่กิน" ในหน้าการนอน ตอบว่ากาแฟบ่ายหรือมื้อดึกทำให้นอนแย่จริงไหม โดยใช้ข้อมูลที่บันทึกอยู่แล้ว

- **`lib/data/sleep_nutrition_insights.dart`** — `compare(nights, mealRepo, sleepTargetMinutes:)` คืน `SleepComparison` ต่อพฤติกรรม (คาเฟอีนหลังบ่าย 2 / มื้อดึกหลัง 2 ทุ่ม)
  - ดูมื้อของ **วันที่เข้านอน** (`SleepEntry.nightDate`) ไม่ใช่วันที่ตื่น
  - **คืนที่ไม่มีบันทึกมื้ออาหารเลยถูกข้าม** ไม่นับเป็น "ไม่ได้กิน" ไม่งั้นวันที่ลืมบันทึกจะไปโป่งกลุ่ม "ไม่ได้กิน" จนสรุปผิด
  - ต้องมีอย่างละ ≥ 2 คืนถึงจะแสดง (`hasEnoughData`) และต่างกัน ≥ 5 คะแนนถึงจะบอกว่าเกี่ยวข้อง (`isMeaningful`) ไม่งั้นขึ้นว่า "พอ ๆ กัน"
  - **ข้อจำกัดที่ตั้งใจ**: แอปไม่เก็บปริมาณคาเฟอีน จึงเดาจาก *ชื่อเมนู* ที่ผู้ใช้พิมพ์ (`caffeineKeywords` ไทย+อังกฤษ) — พิมพ์ชื่อแปลกจะจับไม่ได้ ถือเป็นตัวช่วยสังเกต ไม่ใช่ข้อสรุปทางการแพทย์
- **ข้อควรจำ (test)**: เทสต์ที่ pump `SleepScreen` ต้องเปิด box `meal_entries` ด้วยแล้ว (แก้ `sleep_screen_test`/`sleep_history_test`/`bedtime_reminder_test`)
- test ใหม่ `test/sleep_nutrition_insights_test.dart` (7 tests) — รวม **173 tests**
- ทดสอบบน emulator แล้ว: การ์ดขึ้นสถานะ "ยังเทียบไม่ได้" ถูกต้อง (ข้อมูลตัวอย่างมีมื้ออาหารไม่กี่วัน) ส่วนสถานะที่เทียบได้มี widget test คุม
  - **เจอข้อจำกัดตอนทดสอบ**: ฟอร์มมื้ออาหารบันทึกได้เฉพาะ *วันนี้* (ไม่มีช่องเลือกวันที่) จึงเติมข้อมูลย้อนหลังเพื่อให้การ์ดนี้มีข้อมูลพอไม่ได้เลย — ถ้าจะทำต่อ ควรเพิ่มช่องวันที่ในฟอร์มมื้ออาหาร (`_openMealForm`) ซึ่งจะช่วยกราฟย้อนหลัง 7 วันด้วย

### รอบ 2026-09-18 (ต่อ) — กราฟการนอนย้อนหลัง 30 คืน

- **`lib/data/sleep_history.dart`** — `SleepHistory.of(repo, days: 30, until:)` คืน `SleepNight` ครบทุกช่อง
  - **คืนที่ยังไม่ได้บันทึกก็มีช่องของตัวเอง** (ต่างจาก `SleepRepository.getRecent` ที่ข้ามไปเลย) ไม่งั้นกราฟจะบีบวันที่ขาดหายจนดูเหมือนนอนครบทุกคืน
  - `stats` / `nightsMeetingTarget` นับเฉพาะคืนที่บันทึกไว้, `weeklyAverages()` คืนค่าเฉลี่ยรายสัปดาห์ (สัปดาห์ที่ว่างได้ null) ไว้ดูแนวโน้มว่าดีขึ้นหรือแย่ลง
- หน้าการนอนมีการ์ด "ย้อนหลัง 30 คืน": แท่งรายคืนสีตามคะแนน (คืนที่ไม่บันทึก = ขีดจาง) + เส้นระดับเป้า + เฉลี่ยต่อคืน + คืนที่ถึงเป้า + ค่าเฉลี่ยรายสัปดาห์ 5 ช่อง
  - กราฟ 7 คืนด้านบนตอบว่า "สัปดาห์นี้เป็นยังไง" การ์ดนี้ตอบว่า "ดีขึ้นหรือแย่ลง"
- **ข้อควรจำ (test)**: ตัวเลขเฉลี่ยซ้ำกับการ์ด 7 คืน — เทสต์ต้อง `find.descendant` จาก `AppCard` ของการ์ด 30 คืน
- test ใหม่ `test/sleep_history_test.dart` (5 tests) — รวม **166 tests**
- ทดสอบบน emulator แล้ว: ข้อมูลจริง 7 คืนขึ้นแท่งสีตามคะแนน คืนที่ไม่ได้บันทึกเป็นขีดจาง เฉลี่ย 7 ชม. 1 นาที ถึงเป้า 1/7 และค่าเฉลี่ยรายสัปดาห์ 8.0 / 6.9
  - เจอตอนดูจอจริง: เส้นเป้าสีเทาแยกไม่ออกจากขีดคืนที่ไม่ได้บันทึก และถ้าเป้าสูงกว่าทุกคืนเส้นจะไปทับขอบบน → เปลี่ยนเป็นสีหลักแบบจาง ซ่อนเมื่อชนขอบ และสลับคำอธิบายใต้กราฟตามสถานการณ์

### รอบ 2026-09-18 (ต่อ) — เตือนมื้ออาหาร & ดื่มน้ำ

การ์ด "เตือนมื้ออาหาร & ดื่มน้ำ" ในหน้าโภชนาการ — **สองฟีเจอร์นี้เก็บคนละที่โดยตั้งใจ**

- **มื้ออาหาร** (`lib/data/meal_reminder.dart`) — เขียนเป็นกิจกรรมประจำสัปดาห์ 7 วันต่อมื้อ id คงที่ (`meal-reminder-{breakfast|lunch|dinner}-{1..7}`) แบบเดียวกับ `BedtimeReminder`
  - เปิด/ปิดแยกทีละมื้อ (ค่าเริ่มต้น 07:30 / 12:00 / 18:30) แตะเวลาเพื่อแก้ด้วย time picker และแก้/ลบในหน้าตารางเวลาได้เหมือนกิจกรรมทั่วไป
- **ดื่มน้ำ** (`AppSettings.waterRemindersEnabled/waterIntervalHours/waterStartHour/waterEndHour`) — **ไม่ลงตารางเวลา** เพราะเตือนทุก 2 ชม. = 42 รายการต่อสัปดาห์ จะทำให้ตารางรกจนใช้งานจริงไม่ได้
  - `AppSettings.waterReminderTimes` ไล่เวลาจาก start → end ตามระยะห่าง, `NotificationService._scheduleWaterReminders` ตั้งเป็นการแจ้งเตือน**รายวัน** (`DateTimeComponents.time`) id เริ่มที่ **7000** (ตารางเวลาใช้ 0.., เลื่อนเตือนใช้ 5000+, ทดสอบ 9999)
  - เป็นอิสระจากสวิตช์ "เตือนตามตารางเวลา" — เปิดเฉพาะเตือนน้ำอย่างเดียวได้ (`syncScheduleReminders` จึงตั้งน้ำก่อนแล้วค่อยเช็คสวิตช์ตาราง)
  - เตือนน้ำมีแต่การแจ้งเตือนของระบบ ไม่มีป๊อปอัปในแอป (ป๊อปอัปผูกกับ `ScheduleEvent`)
- **ข้อควรจำ (test)**: เทสต์ที่ pump `NutritionScreen` ต้องเปิด box `app_settings` ด้วย (แก้ `nutrition_screen_test`/`nutrition_history_test` แล้ว)
- test ใหม่ `test/meal_water_reminder_test.dart` (9 tests) — รวม **161 tests**
- ทดสอบบน emulator แล้ว: เปิดมื้อเย็น → ตารางเวลาขึ้น "18:30 มื้อเย็น • เตือนมื้ออาหาร" และการ์ดโชว์ "18:30 ✏️" แก้เวลาได้, เปิดเตือนน้ำได้ "09:00–19:00 น. • 6 ครั้ง/วัน" เปลี่ยนเป็นทุก 3 ชม. แล้วเหลือ 4 ครั้ง/วัน, ปิดทั้งคู่แล้วตารางกลับมาว่างเหมือนเดิม

### รอบ 2026-09-18 (ต่อ) — นัดลูกค้าจากหน้า CRM

ต่อยอดจากนัดหมายเฉพาะวันที่ — หน้าลูกค้าสร้างนัดลงตารางเวลาได้ตรง ๆ

- **`ScheduleEvent.clientId`** (String?, null = กิจกรรมทั่วไป) — ผูกนัดกับลูกค้าด้วย **id ไม่ใช่ชื่อ** เพราะผู้ใช้เปลี่ยนชื่อลูกค้าได้ (ตรงกับ `linkedEntityId` ที่ DATA_MODEL ออกแบบไว้); ไฟล์สำรองเก่าอ่านได้เหมือนเดิม
- **Repository**: `upcomingForClient(clientId, from:)` / `nextForClient(...)` — นับนัด**ของวันนี้ว่ายังมาไม่ถึง** และข้ามนัดที่ผ่านไปแล้ว
- **หน้าลูกค้า**: ฟอร์มแก้ไขมีปุ่ม "นัดหมายลูกค้ารายนี้" → เลือกวันที่/เวลา/รายละเอียด (เติมความสนใจของลูกค้าให้ก่อน) → สร้างกิจกรรมเฉพาะวันที่หมวดลูกค้า + `syncScheduleReminders()`
  - แถวรายชื่อโชว์ "นัด 25 ก.ย. 13:30 น." ของนัดถัดไป และ rebuild เมื่อตารางเวลาเปลี่ยน (เพิ่ม `scheduleRepo.listenable()` เข้า `Listenable.merge`)
- **`PickerBox`** ย้ายมาเป็น widget กลางใน `form_sheet.dart` (หน้าตาเดียวกับ AuthField แต่เปิด picker) + `CalendarUtils.thaiDateShort`
- **ข้อควรจำ (test)**: เทสต์ที่ pump `CrmScreen` ต้องเปิด box `schedule_events` ด้วย (แก้ `crm_screen_test`/`edit_forms_test` แล้ว)
- test ใหม่ `test/client_appointment_test.dart` (6 tests) — รวม **152 tests**
- ทดสอบบน emulator แล้ว: กดดินสอ → "นัดหมายลูกค้ารายนี้" → เลือก 23 ก.ย. → บันทึก แล้วแถวลูกค้าขึ้น "นัด 23 ก.ย. 10:00 น." และตารางเวลาวันที่ 23 มี "นัด คุณสมชาย ใจดี" พร้อมป้ายนัดเฉพาะวัน (ลบนัดทดสอบออกแล้ว)

### รอบ 2026-09-18 (ต่อ) — เตือน "ถึงเวลาเข้านอน" อัตโนมัติ

- **`lib/data/bedtime_reminder.dart`** — เวลาเข้านอนที่แนะนำ = **เวลาตื่นเฉลี่ยจริง − เป้าเวลานอน**
  - เวลาตื่นก่อน 03:00 บวก 24 ชม. ก่อนเฉลี่ย (ตื่น 01:00 กับ 07:00 ต้องได้ 16:00 ไม่ใช่ 04:00) — หลักเดียวกับเวลาเข้านอนใน `SleepStats`
  - เปิดใช้ = เขียนกิจกรรมประจำสัปดาห์ 7 รายการ **id คงที่** (`bedtime-auto-1..7`, seriesId `bedtime-auto`) จึงได้ทั้งการแจ้งเตือนของระบบและป๊อปอัปในแอปโดยไม่ต้องเขียนอะไรเพิ่ม และกดซ้ำก็ไม่มีรายการค้าง
  - ผู้ใช้ยังแก้/ลบรายการนี้เองในหน้าตารางเวลาได้ตามปกติ
- หน้าการนอนมีการ์ด "เตือนให้เข้านอน" (สวิตช์) — ยังไม่มีบันทึกการนอน = สวิตช์กดไม่ได้พร้อมบอกเหตุผล, ถ้าเวลาตื่นเฉลี่ยขยับจะมีปุ่ม "ปรับเป็น HH:mm" ให้กดเอง (ไม่แก้เงียบ ๆ)
- **ข้อควรจำ (test)**: เทสต์ที่ pump `SleepScreen` ต้องเปิด box `schedule_events` ด้วย (แก้ `sleep_screen_test` แล้ว)
- test ใหม่ `test/bedtime_reminder_test.dart` (9 tests) — รวม **146 tests**
- ทดสอบบน emulator แล้ว: การ์ดคำนวณได้ 22:41 น. จากข้อมูลการนอนจริง, เปิดสวิตช์แล้วขึ้น "เตรียมตัวเข้านอน 22:41" ครบทุกวันในตารางเวลา, ปิดแล้วหายครบและกิจกรรมอื่นไม่หาย

### รอบ 2026-09-18 (ต่อ) — กราฟโภชนาการย้อนหลัง 7 วัน

- **`lib/data/nutrition_history.dart`** — `NutritionHistory.of(mealRepo, waterRepo, today:)` รวมมื้ออาหาร + น้ำดื่มของ 7 วัน (เก่า → ใหม่)
  - **ค่าเฉลี่ยนับเฉพาะวันที่บันทึกไว้จริง** (`loggedDays`) ไม่งั้นวันที่ยังไม่ได้ใช้แอปจะถ่วงค่าเฉลี่ยให้ต่ำจนดูเหมือนกินน้อย
  - วันที่ดื่มน้ำอย่างเดียว (ไม่บันทึกมื้อ) ก็นับว่าบันทึกแล้ว
  - ลบวันด้วย `DateTime(y, m, d - i)` ไม่ใช้ `Duration` (เหตุผลเดียวกับ `CalendarUtils`)
- หน้าโภชนาการมีการ์ด "ย้อนหลัง 7 วัน": แท่งพลังงานรายวัน (แดง = เกินเป้า, จาง = ยังไม่บันทึก), เฉลี่ย kcal/วัน, วันที่ไม่เกินเป้า, น้ำเฉลี่ย
- test ใหม่ `test/nutrition_history_test.dart` (6 tests) — รวม **137 tests**
- ทดสอบบน emulator แล้ว: วันที่มีบันทึกขึ้นแท่ง 1,135 kcal วันที่ยังไม่บันทึกขึ้น "–" และหัวการ์ดโชว์ค่าเฉลี่ยถูกต้อง

### รอบ 2026-09-18 — ชั้น AuthService (เตรียมต่อระบบล็อกอินจริง)

ยังไม่ได้ต่อ Firebase/Google/Apple จริง (ผู้ใช้เลือก "เตรียมโค้ดไว้ก่อน") — งานรอบนี้คือแยกการยืนยันตัวตนออกจากหน้าจอ
เพื่อให้วันที่จะต่อของจริง แก้แค่ไฟล์เดียว

- **`lib/data/auth/auth_service.dart`** — abstract `AuthService` (`currentUser`, `signInWithEmail`, `registerWithEmail`, `signInWithProvider`, `signOut`, `isReal`) + `AuthResult` (สำเร็จ/ข้อความ error ภาษาไทย) + `AuthValidation` (อีเมล/รหัสผ่าน/ยืนยันรหัสผ่าน)
  - หัวไฟล์มี **ขั้นตอนต่อ Firebase Auth 5 ข้อ** เขียนไว้ครบ — เริ่มจากตรงนั้นได้เลย
  - จุดสลับ: `AuthService.instance = FirebaseAuthService();` ใน `main()` ก่อน `runApp` (หน้าจอไม่ต้องแก้)
- **`lib/data/auth/local_auth_service.dart`** — พฤติกรรมเดิม (บัญชีในเครื่องผ่าน Hive) แต่ย้ายมาอยู่หลัง interface
  - **รหัสผ่านยังไม่ถูกเก็บ/ตรวจจริง** ตรวจแค่รูปแบบ (≥ 8 ตัว) ให้ UX เหมือนของจริง
  - ล็อกอินด้วยอีเมลเดิม (ไม่สนตัวพิมพ์เล็ก-ใหญ่) = บัญชีเดิม ชื่อ/ข้อมูลไม่หาย — อีเมลใหม่ = บัญชีใหม่
- **หน้าจอ**: `login_screen`/`register_screen`/`profile_screen`/`main.dart` เรียกผ่าน `AuthService.instance` ไม่แตะ `UserRepository` ตรง ๆ แล้ว
  - หน้าเข้าสู่ระบบมีแถบ "โหมดทดลอง: บัญชีเก็บในเครื่องนี้เท่านั้น…" ซึ่ง **หายเองเมื่อ `isReal` เป็น true**
  - กันกดปุ่มซ้ำระหว่างรอด้วย `_busy` (ของจริงมี network latency)
- test ใหม่ `test/auth_service_test.dart` (9 tests) — `flutter test` ผ่าน **131 tests**
- ทดสอบบน emulator แล้ว: ออกจากระบบ → หน้าเข้าสู่ระบบขึ้นแถบโหมดทดลอง, กดเข้าสู่ระบบทั้งที่ยังไม่กรอก/อีเมลผิดรูปแบบ/รหัสสั้น ขึ้นข้อความถูกต้องทั้ง 3 แบบ, กรอกครบแล้วเข้าแอปได้ และปุ่ม Google ยังใช้บัญชีในเครื่องเหมือนเดิม

### รอบ 2026-09-17 (ต่อ) — นัดหมายเฉพาะวันที่

เวอร์ชันแอป **1.5.0+6** — รายละเอียดโครงสร้างข้อมูลดู DATA_MODEL หัวข้อ 8 (`date`)

- **`ScheduleEvent.date`** (null = ประจำสัปดาห์แบบเดิม) + `ScheduleEvent.oneOff(...)`, `isOneOff`, `occursOn(day)`
- **Repository**: `getForDate(day)` = ทุกอย่างที่เกิดในวันนั้น, `ScheduleRepository.eventsOn(all, day)` (static ใช้กับรายการที่อ่านไว้แล้ว — หน้าปฏิทินเรียกหลายสิบช่อง/ครั้ง), `getByWeekday`/`getWeek` **เหลือเฉพาะประจำสัปดาห์**
  - **ข้อควรจำ**: โค้ดใหม่ที่อยากได้ "กิจกรรมของวันนี้" ต้องใช้ `getForDate` ไม่ใช่ `getByWeekday` (แก้หน้าหลักและหน้าโภชนาการแล้ว)
- **หน้าตารางเวลา**: ทุกมุมมองเปลี่ยนจาก `week[weekday - 1]` เป็นฟังก์ชัน `_EventsOn` ตามวันที่จริง
  - ฟอร์มมีปุ่มสลับ **ทุกสัปดาห์ / เฉพาะวันที่** (`ValueKey('repeat-weekly' / 'repeat-one-off')`) — เฉพาะวันที่แสดงช่องเลือกวันที่ (`showDatePicker`) และซ่อนเลือกหลายวัน/คัดลอก/แก้ทั้งชุด
  - เพิ่มจาก **มุมมองเดือน** ค่าเริ่มต้นเป็นเฉพาะวันที่ (วันที่แตะในปฏิทิน) ส่วนมุมมองอื่นเริ่มที่ทุกสัปดาห์เหมือนเดิม
  - แปลงกิจกรรมในชุดเป็นนัดเฉพาะวัน → หลุดจากชุด (วันอื่นยังอยู่), แปลงกลับเป็นประจำสัปดาห์ได้
  - นัดเฉพาะวันมีป้าย "นัดหมายเฉพาะวันนี้" (มุมมองวัน/เดือน) และไอคอนปฏิทินเล็ก (มุมมองสัปดาห์); "คัดลอกทั้งวัน" คัดลอกเฉพาะกิจกรรมประจำ
- **แจ้งเตือน**: `NotificationService` ตั้งครั้งเดียวและข้ามนัดที่เลยแล้ว, `ReminderWatcher.lastRemindAtOrBefore` ไม่ย้อนหารอบสัปดาห์ก่อน, ป๊อปอัปแสดงวันที่เต็ม
- test ใหม่ `test/one_off_event_test.dart` (13 tests) — `flutter test` ผ่าน **122 tests**, `flutter analyze` ไม่มีปัญหา
- ทดสอบบน emulator (release APK x86_64) แล้ว: เพิ่มจากมุมมองเดือนวันที่ 25 → ฟอร์มเลือก "เฉพาะวันที่" + วันที่ 25 ให้เอง, บันทึกแล้วขึ้นป้ายนัดหมาย, ศุกร์ 2 ต.ค. ว่าง, กล่องยืนยันลบแสดงวันที่เต็ม, ลบแล้วหายจริง

### รอบ 2026-09-17 — แก้ไข/ลบรายการในหน้าอื่น

ทุกหน้าที่เดิมเพิ่มได้อย่างเดียวตอนนี้แก้ไขและลบได้แล้ว ใช้ฟอร์มเดียวกับตอนเพิ่ม (`_open…Form(existing:)`)

- **ตัวช่วยกลางใหม่** ใน `lib/widgets/form_sheet.dart`
  - `confirmDeleteDialog()` — กล่องยืนยันลบ
  - `FormDeleteButton` — ปุ่มลบสำหรับ `footerBuilder` (ยืนยัน → ปิดฟอร์ม → ลบ → SnackBar บนหน้าเดิม)
  - `RowEditButton` — ไอคอนดินสอท้ายแถว ใช้กับรายการที่ "แตะ" ถูกใช้ทำอย่างอื่นอยู่แล้ว
  - หน้าโภชนาการ/ตารางเวลายังใช้โค้ดลบของตัวเองอยู่ ย้ายมาใช้ตัวช่วยกลางได้ภายหลัง
- **วิธีเปิดฟอร์มแก้ไขแต่ละหน้า**
  - งานประจำ: ไอคอนดินสอ (แตะการ์ด = ติ๊กเสร็จเหมือนเดิม) — ฟอร์มเลือก **สถานะ** ได้แล้ว รวม "กำลังทำ" + ช่องความคืบหน้า % (เดิมไม่มีทางตั้งงานเป็นกำลังทำจากแอปเลย); ปัดซ้ายลบต้องยืนยันก่อนแล้ว
  - ออกกำลังกาย: ไอคอนดินสอ (แตะ = ติ๊กเสร็จ) — ชื่อเก็บเป็น `"วัน • ชื่อ"` ฟอร์มจึงตัดคำนำหน้าวันออกก่อน (`ExerciseScreen.plainTitle`) กันชื่อซ้อนเป็น "พุธ • จันทร์ • …"; คง `isDone`/`isToday` เดิมไว้
  - การเงิน: แตะแถวรายการ — **วันที่เดิมไม่เปลี่ยน**; รายรับบังคับหมวด `other`; จำนวนเงินพิมพ์ `1,250` ได้
  - เรียนรู้: แตะการ์ด (ปุ่ม "เรียนต่อ" ยังแยกทำงาน) — แก้ความคืบหน้า % ได้ (clamp 0–100) คง `isPrimary`
  - ลูกค้า: ไอคอนดินสอ (แตะ = เลื่อนสถานะ) — เลือกสถานะย้อนกลับได้ (เดิมแตะเกินแล้วย้อนไม่ได้), แก้รายละเอียด `policyLabel` ทั้งข้อความ, ชื่อย่อคำนวณใหม่จากชื่อ
- test ใหม่ `test/edit_forms_test.dart` (10 tests) — หน้าลูกค้าต้อง `scrollUntilVisible` ก่อน เพราะรายชื่ออยู่ใต้รายงานประจำสัปดาห์
- แก้ `schedule_screen_test` "มุมมองเดือน…" ที่ล้มเฉพาะบางวัน (วันที่รันตรงกับวันที่มีกิจกรรม → หน้ายาวขึ้น → ปุ่ม "ถัดไป" หลุดจอ) โดย `ensureVisible` ก่อนแตะ
- `flutter analyze` ไม่มีปัญหา, `flutter test` ผ่าน **109 tests**

### รอบ 2026-09-16 (ต่อ) — ปฏิทินรายเดือน/รายปี + คัดลอกกิจกรรมประจำ (บันทึกละเอียดที่ `16092026 ปฏิทินและคัดลอกกิจกรรม.md`)

หน้า `lib/screens/schedule_screen.dart` เขียนใหม่เกือบทั้งไฟล์ — เวอร์ชันแอป **1.4.0+5**

- **มุมมอง 4 แบบ** วัน / สัปดาห์ / เดือน / ปี (ปุ่มสลับย้ายลงมาเป็นแถวเต็มความกว้างใต้หัวข้อ เพราะ 4 ปุ่มไม่พอที่บนจอ 360dp)
  - state หลักเปลี่ยนจาก `_selectedWeekday` เป็น `_selectedDate` (วันที่จริง) + `_visibleMonth` — แถบ 7 วันแสดงสัปดาห์ของวันที่เลือก มีลูกศรเลื่อนสัปดาห์และปุ่ม "วันนี้"
  - เดือน: ปฏิทินเริ่มวันจันทร์ จุดสีตามหมวดของกิจกรรมวันนั้น แตะวันที่ → รายการใต้ปฏิทิน / แตะชื่อเดือน → มุมมองปี
  - ปี: 12 เดือนย่อ (2 คอลัมน์บนมือถือ, 3 คอลัมน์ถ้ากว้าง ≥ 520) มุมขวาบอกจำนวนกิจกรรมทั้งเดือน
  - ~~ตารางยังเป็นประจำสัปดาห์ล้วน~~ → เพิ่มนัดหมายเฉพาะวันที่แล้ว (2026-09-17) ดูรอบด้านบน
- **ตัวช่วยปฏิทิน** `lib/data/calendar_utils.dart` — `monthGrid` (28/35/42 ช่อง), `startOfWeek`, `addDays`, `addMonths`, ชื่อเดือนไทย + ปี พ.ศ.
  - บวกวันด้วย `DateTime(y, m, d + n)` ไม่ใช้ `add(Duration(days:))` เพราะจะคลาด 1 ชม. ในวันเปลี่ยนเวลาออมแสง
- **คัดลอก/ชุดกิจกรรม** — `ScheduleEvent.seriesId` + `seriesKey` (ดู DATA_MODEL หัวข้อ 8)
  - เพิ่มใหม่: เลือกได้หลายวัน (ทุกวัน / จ–ศ / ส–อา) → สร้างรายการแยกวันละ 1 รายการในชุดเดียวกัน
  - แก้ไข: ย้ายวัน, "คัดลอกไปวันอื่นด้วย" (วันที่มีชุดนี้อยู่แล้วเป็นสีจาง), สวิตช์ "ใช้การแก้ไขกับทุกวันในชุดนี้" (ปิดไว้เป็นค่าเริ่มต้น = แก้เฉพาะวันนี้)
  - ลบจากฟอร์ม: ถ้ามีหลายวันจะถาม ลบเฉพาะวันนี้ / ลบทุกวัน — ส่วนปัดซ้ายลบเฉพาะวันนั้นเสมอ
  - มุมมองวัน/เดือนมีปุ่ม "คัดลอกทั้งวัน" (เช่น ตารางวันจันทร์ → อังคาร–ศุกร์)
  - ทุกการเปลี่ยนแปลงเรียก `NotificationService.syncScheduleReminders()` ต่อ — การแจ้งเตือนไม่ต้องแก้เพราะแต่ละวันเป็นเรคคอร์ดแยกอยู่แล้ว
- แก้บั๊กเดิม: มุมมองสัปดาห์เลื่อนแนวตั้งไม่ได้ วันที่มีกิจกรรมเยอะจะล้นจอ (ตอนนี้เลื่อนได้ทั้งสองแนว)
- `flutter test` ผ่าน **99 tests** (เพิ่ม 13: `calendar_utils_test` 4, `repository_test` 4, `schedule_screen_test` 5)
  - **ข้อควรจำ (test)**: ฟอร์มยาวเกินจอทดสอบ 800x600 แล้ว — `tapVisible` ต้อง `ensureVisible` 2 รอบ เพราะช่องข้อความที่เพิ่งพิมพ์จะเลื่อนตัวเองกลับมาหลังรอบแรก

### รอบ 2026-09-16 (ต่อ) — โมดูลคุณภาพการนอน

โมดูลที่ 7 ของแอป เข้าจากการ์ดเต็มความกว้างบนหน้าหลัก (`lib/screens/sleep_screen.dart`)

- **`SleepEntry`** (`lib/models/sleep_entry.dart`) — คืนละหนึ่งรายการ โดยใช้ **วันที่ตื่น** เป็น id (`yyyy-MM-dd`) แบบเดียวกับ `WaterLog` บันทึกซ้ำจึงทับของเดิมเสมอ
  - `durationMinutes` วนข้ามเที่ยงคืนด้วย `(wake - bed + 1440) % 1440` — อย่าลบตรง ๆ
  - `nightDate` = วันที่เข้านอน (เข้านอนตั้งแต่เที่ยงวันขึ้นไป = คืนของวันก่อนหน้า) ใช้แสดงว่า "คืนวันอังคาร"
  - `SleepStats.of()` สรุป 7 คืน: เวลานอน/คุณภาพ/การตื่นกลางดึกเฉลี่ย + **ความสม่ำเสมอของเวลาเข้านอน** + ปัจจัยรบกวนที่เจอบ่อย
  - **จุดที่พลาดง่าย**: เวลาเข้านอนหลังเที่ยงคืน (01:00) ต้อง +24 ชม. ก่อนหาค่าเฉลี่ย ไม่งั้น 23:00 กับ 01:00 จะถูกมองว่าห่างกัน 22 ชั่วโมงแทนที่จะเป็น 2 — มี test คุมไว้
- **`Insights.scoreOfNight()`** — เฉลี่ย 3 ส่วน: เวลานอน/เป้า, น้ำหนักคุณภาพที่ผู้ใช้ให้, และคะแนนการตื่นกลางดึก (ครั้งละ −15% พื้น 40%) และ `Insights.sleepScore()` ป้อนคะแนนเข้า `categoryScores()`
- **`LifeCategory.sleep`** ถูกเพิ่มเข้า enum (ระหว่าง `nutrition` กับ `work`) → หน้าตารางเวลาเลือกหมวดนี้ได้ทันที จึงตั้งเตือน "เตรียมตัวเข้านอน" แล้วได้ป๊อปอัป+เสียงจากรอบที่แล้วฟรี ๆ
  - เพิ่มค่าใน enum ปลอดภัยเพราะทุก `fromMap` ใช้ `firstWhere(..., orElse:)` และเก็บเป็นชื่อสตริง
- box ใหม่ `sleep_entries` — เพิ่มแล้วทั้งใน `HiveBoxes.init()`, `BackupService._boxNames` (อยู่ในไฟล์สำรอง) และ `recordBoxNames` (โดนล้างตอน "ล้างประวัติทั้งหมด")
  - **ข้อควรจำ**: เทสต์ทุกไฟล์ที่เปิด box เองต้องเพิ่ม `HiveBoxes.sleepEntries` ด้วย ไม่งั้น `Box not found` (แก้ `backup_test` / `repository_test` / `widget_test` / `dashboard_screen_test` ไปแล้ว)
- เป้าหมายใหม่ `GoalSettings.sleepTargetMinutes` (ค่าเริ่มต้น 480 = 8 ชม.) กรอกเป็น **ชั่วโมง** ในหน้าตั้งค่าเป้าหมาย แต่เก็บเป็นนาที
- `SleepScreen.tipsFor()` เป็น static ล้วน (รับ `SleepStats` + `GoalSettings`) จึงเทสต์คำแนะนำได้โดยไม่ต้อง pump หน้าจอ
- test ใหม่: `test/sleep_test.dart` + `test/sleep_screen_test.dart` — รวมชุดทดสอบเป็น **86 tests**
  - เทสต์ที่ใช้ memory box (`bytes: Uint8List(0)`) ต้อง `await Hive.close()` ก่อน `tearDownTestHive()` ไม่งั้นได้ `Unsupported operation ... for memory boxes`
  - รายการในหน้าจอถูกสร้างแบบ lazy ตามที่เลื่อนถึง — ต้อง `scrollUntilVisible` / `ensureVisible` ก่อน tap (ทั้งในหน้าและในบอตทอมชีต)

### รอบ 2026-09-16 (ต่อ) — ล้างข้อมูลเพื่อเริ่มเก็บประวัติใหม่

เพิ่มในหน้า `lib/screens/backup_screen.dart` (โปรไฟล์ → สำรอง & กู้คืนข้อมูล) ส่วนล่างสุด "เริ่มเก็บประวัติใหม่"

- `BackupService.clearRecords()` — ล้างเฉพาะ `recordBoxNames` (งาน, ออกกำลังกาย, มื้ออาหาร, น้ำดื่ม, ลูกค้า, รายงานสัปดาห์, การเงิน, การเรียนรู้, ตารางเวลา, สตรีค) คืนค่าจำนวนรายการที่ลบ — บัญชีผู้ใช้/เป้าหมาย/การตั้งค่ายังอยู่
- `BackupService.clearEverything()` — ล้างทุก box แล้วเด้งกลับหน้า Login (ยืนยัน 2 ชั้น)
- `BackupService.currentCounts()` — ใช้โชว์ในกล่องยืนยันว่ากำลังจะลบอะไรไปกี่รายการ
- **กันข้อมูลตัวอย่างกลับมา**: `AppSettings.sampleDataDisabled` ถูกตั้งเป็น true หลังล้างทุกครั้ง และ `SeedData.seedIfEmpty()` เช็คธงนี้เป็นอย่างแรกแล้ว return ทันที — ถ้าไม่มีธงนี้ ข้อมูลตัวอย่าง (ลูกค้าสมมติ ฯลฯ) จะโผล่กลับมาตอนเปิดแอปครั้งถัดไปเพราะ box ว่าง
  - `clearEverything()` ล้าง `app_settings` ไปด้วย จึงต้องเขียนธงกลับลงไป **หลัง** ล้างเสร็จ (ดู `_disableSampleData()`)
- หลังล้างต้องเรียก `NotificationService.syncScheduleReminders()` เพื่อยกเลิกการแจ้งเตือนของกิจกรรมที่ถูกลบไปแล้ว
- `flutter test` ผ่าน **46 tests** (เพิ่ม 4 เคสใน `test/backup_test.dart`)
- **ข้อควรจำ**: `seedIfEmpty()` อ่าน box `app_settings` แล้ว เทสต์ไหนที่เรียก `SeedData.seedIfEmpty()` ต้องเปิด box นี้ด้วย (แก้ `widget_test.dart` ไปแล้ว)

### รอบ 2026-09-16 (ต่อ) — รายงานผลงานประจำสัปดาห์ P-A-S-R-F-N-T ในโมดูลลูกค้า & ขายประกัน

ส่วนใหม่ในหน้า `lib/screens/crm_screen.dart` (คลาส `WeeklyReportSection` อยู่ท้ายไฟล์เดียวกัน) สำหรับสรุปงานขายรายสัปดาห์ส่งหัวหน้า

- **ข้อมูล**: `WeeklyReport` (`lib/models/weekly_report.dart`) — 1 รายการต่อ 1 สัปดาห์ คีย์ = `yyyy-MM-dd` ของวันจันทร์ (`WeeklyReport.weekKeyOf`) จึงบันทึกซ้ำทับสัปดาห์เดิมเสมอ เก็บ `ownerName`, `activities` (Map ของ `ActivityCode` → `WeeklyActivity{count, note}`) และ `salesPremium` → box ใหม่ `weekly_reports` (อยู่ในไฟล์สำรองแล้ว, `backupFormatVersion` ยังเป็น 1)
- **ตัวย่อ** `ActivityCode`: P=Prospect, A=Appointment, S=Sales, R=Referal, F=Follow, N=New Market, T=Team
- **ข้อความรายงาน** `WeeklyReport.toReportText()` ประกอบหัวเรื่อง + คำอธิบายตัวย่อ + บรรทัดของทุกตัวย่อ เช่น `A = 3 (ลูกค้าใหม่ 3)` และบรรทัด S พ่วงเบี้ยประกันให้เอง (`S = 2 ราย เบี้ยประมาณ 60,000 บาท (ขาย Offline)`) — ปุ่ม "คัดลอกส่งหัวหน้า" ยัดข้อความนี้ลงคลิปบอร์ด
- **วันที่แบบไทย** `WeeklyReport.thaiShortDate()` = `29/4/67` (พ.ศ. 2 หลักท้าย) ใช้ทั้งบนการ์ดและในข้อความ
- เลื่อนสัปดาห์ด้วยลูกศร ‹ › (ล็อกไม่ให้เลยสัปดาห์ปัจจุบัน) และปุ่ม "ย้อนหลัง" เปิด bottom sheet รายการรายงานที่บันทึกไว้ทั้งหมด
- `seed_data.dart` ใส่รายงานตัวอย่างของสัปดาห์ 29/4/67-5/5/67 ไว้ให้ตั้งแต่เปิดแอปครั้งแรก
- `flutter analyze` ไม่มีปัญหา, `flutter test` ผ่าน **42 tests** (เพิ่ม 2 unit test ใน `repository_test.dart` + `test/crm_screen_test.dart` 3 เคส)
- **ข้อควรจำ**: เพิ่ม box ใหม่แล้วต้องไปเปิด box นั้นใน `setUp` ของ `backup_test.dart`, `widget_test.dart`, `dashboard_screen_test.dart` ด้วย ไม่งั้นเทสต์เดิมพังเพราะ seed หา box ไม่เจอ
- **APK**: build แล้วที่ `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` (18.8 MB) เซ็นด้วย release key จริง (SHA-256 `249a7ed3…`) และ bump `pubspec.yaml` เป็น `1.1.0+2` → ติดตั้งทับตัวก่อน (1.0.0+1) ได้โดยข้อมูลไม่หาย
- **กฎการอัปเดต APK**: ทุกครั้งที่จะแจก APK ใหม่ต้อง **bump build number ใน `pubspec.yaml`** (เลขหลัง `+`) ไม่งั้น Android ปฏิเสธการติดตั้งทับ — และต้องใช้ keystore เดิมเสมอ (`android/lifeplan-release.jks`) ห้ามปล่อยให้ fallback ไป debug key เพราะจะลงทับของเดิมไม่ได้

### รอบ 2026-09-16 — โมดูลทานอาหาร & โภชนาการ (บันทึกละเอียดที่ `16092026 โภชนาการ.md`)

โมดูลใหม่ทั้งชุด เข้าจากการ์ด "ทานอาหาร & โภชนาการ" ในหน้าหลัก (`lib/screens/nutrition_screen.dart`)

- **ข้อมูล**: `MealEntry` (มื้อ, เวลา, kcal, โปรตีน, แป้ง, ไขมัน, น้ำตาล, วิตามิน A–K, ก่อน/หลังออกกำลังกาย) + `WaterLog` (น้ำดื่มวันละ 1 รายการ คีย์ = `yyyy-MM-dd`, แก้วละ 250 มล.) → box ใหม่ `meal_entries`, `water_logs` (อยู่ในไฟล์สำรองแล้ว, `backupFormatVersion` ยังเป็น 1 เพราะไฟล์เก่าอ่านได้ตามเดิม)
- **เป้าหมาย**: `GoalSettings` เพิ่ม `calorieTarget`, `proteinTarget`, `carbTarget`, `fatTarget`, `sugarLimit`, `waterTargetMl` — แก้ได้ในหน้า ตั้งค่าเป้าหมาย (เรคคอร์ดเก่าที่ยังไม่มีฟิลด์เหล่านี้ใช้ค่าเริ่มต้นอัตโนมัติ)
- **หมวดชีวิตใหม่** `LifeCategory.nutrition` (สีชมพูแดง) — โผล่ในตารางเวลา, กราฟหน้า Progress และ `Insights.categoryScores()` ด้วย
- **คะแนนโภชนาการ** `Insights.nutritionScore()` = เฉลี่ยของ พลังงาน/เป้า, โปรตีน/เป้า, น้ำ/เป้า และคะแนนน้ำตาล (เกินเพดานยิ่งมากยิ่งลด)
- **เชื่อมกับออกกำลังกาย**: การ์ดล่างสุดของหน้าโภชนาการเทียบพลังงานที่กินกับที่เผาผลาญวันนี้ (7 kcal/นาที เท่ากับหน้าออกกำลังกาย), ดึงเวลาออกกำลังกายของวันนี้จากตารางเวลา และเช็คว่ามีมื้อก่อน/หลังออกกำลังกายครบหรือยัง
- แก้ไข/ลบมื้ออาหารได้ด้วยแพตเทิร์น `footerBuilder` เดียวกับหน้าตารางเวลา (ถามยืนยันก่อนลบ)
- `flutter analyze` ไม่มีปัญหา, `flutter test` ผ่าน **37 tests** (เพิ่ม 4 unit test + `test/nutrition_screen_test.dart` 3 เคส + `test/dashboard_screen_test.dart` 1 เคส)
- ยังไม่ได้ทดสอบบน emulator/มือถือจริง และยังไม่มีไฟล์ดีไซน์ `.dc.html` ของหน้านี้

### รอบ 2026-09-15 ค่ำ (เครื่อง `D:\WORK\PROGRAMPLANNER`)

| commit | งาน |
|---|---|
| `7fb005f` | **หน้าตารางเวลาแก้ไขและลบกิจกรรมได้** — แตะกิจกรรม (ทั้งมุมมองรายวันและรายสัปดาห์) เปิดฟอร์มเดิมที่กรอกค่าไว้แล้ว, ปุ่ม "ลบกิจกรรมนี้" ในฟอร์ม, ถามยืนยันก่อนลบทุกครั้ง (รวมตอนปัดซ้าย), ลบแล้วขึ้น SnackBar, ตั้งการแจ้งเตือนใหม่อัตโนมัติ |
| `b1185e6` | `android/gradle.properties`: `kotlin.incremental=false` — แก้ build APK ล้ม (ดูข้อ 4.15) |
| `29cf8f6` | บันทึก `14092026.txt` |

- `showAppFormSheet` (`lib/widgets/form_sheet.dart`) มีพารามิเตอร์ใหม่ `footerBuilder` สำหรับปุ่มรองใต้ปุ่มบันทึก — หน้าอื่นที่อยากทำ "แก้ไข/ลบ" ใช้แพตเทิร์นเดียวกับ `_openEventForm` ใน `schedule_screen.dart` ได้เลย
- `flutter analyze` ไม่มีปัญหา, `flutter test` ผ่าน **29 tests**
- APK ล่าสุด (มีฟีเจอร์แก้ไข/ลบ) อยู่ที่ `lifeplan_app/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk` — **เซ็นด้วย debug key** เพราะเครื่องนี้ไม่มี `lifeplan-release.jks` / `key.properties` (อยู่บนเครื่องเดิม `D:\PROGRAMPLANNER`) ใช้ทดสอบได้ แต่ห้ามแจกจ่าย และติดตั้งทับแอปที่เซ็นด้วย release key ไม่ได้
- ยังไม่ได้ทดสอบฟีเจอร์แก้ไข/ลบบนมือถือ/emulator จริง (มีแค่ widget test)

**ความต่างของเครื่องนี้กับเครื่องเดิม:** โปรเจกต์อยู่ที่ `D:\WORK\PROGRAMPLANNER` (ไม่ใช่ `D:\PROGRAMPLANNER`), ไม่ได้ตั้ง `PUB_CACHE`/`GRADLE_USER_HOME` → pub cache อยู่ที่ `C:\Users\naris\AppData\Local\Pub\Cache`, เปิด Windows Developer Mode แล้ว

---

## 1. คำสั่งเริ่มงานต่อ (copy-paste ได้เลย)

```bash
cd D:/PROGRAMPLANNER/lifeplan_app

# ตั้ง environment (ถ้าเปิด terminal ใหม่ที่ยังไม่มี persistent PATH)
export JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-17.0.20.101-hotspot"
export ANDROID_HOME="D:\android-sdk"
export ANDROID_SDK_ROOT="D:\android-sdk"
export ANDROID_AVD_HOME="D:\android-avd"
export GRADLE_USER_HOME="D:\gradle-home"
export PUB_CACHE="D:\pub-cache"
export PATH="/d/flutter/bin:/c/Program Files/Eclipse Adoptium/jdk-17.0.20.101-hotspot/bin:/d/android-sdk/cmdline-tools/latest/bin:/d/android-sdk/platform-tools:/d/android-sdk/emulator:$PATH"

flutter pub get
flutter analyze                 # ต้องขึ้น "No issues found!"
flutter test                    # ต้องผ่านทั้ง 29 tests

# รันบนเว็บ (เร็วสุดสำหรับเช็ค UI)
flutter build web --release
cd build/web && python -m http.server 8080   # แล้วเปิด http://localhost:8080

# รันบน Android emulator
flutter emulators --launch lifeplan_avd      # บูตครั้งแรกช้า ~1-2 นาที
flutter run -d emulator-5554
```

หมายเหตุ: `ANDROID_HOME`, `ANDROID_SDK_ROOT`, `JAVA_HOME` และการเพิ่ม PATH ถูกตั้งเป็น **persistent user environment variable** ไว้แล้ว (`[Environment]::SetEnvironmentVariable(..., 'User')`) — เทอร์มินัล **ใหม่** (โดยเฉพาะ PowerShell ที่เปิดใหม่) ควรเห็นค่าพวกนี้อัตโนมัติโดยไม่ต้อง export เอง แต่ Git Bash session เดิมที่เปิดค้างจะไม่เห็นจนกว่าจะเปิดใหม่

---

## 2. Environment ที่ติดตั้งไว้บนเครื่องนี้

| อย่าง | ที่อยู่ / เวอร์ชัน |
|---|---|
| Flutter SDK | `D:\flutter` (stable, 3.47.4) — ติดตั้งด้วย `git clone -b stable --depth 1` |
| JDK | `C:\Program Files\Eclipse Adoptium\jdk-17.0.20.101-hotspot` (Temurin 17, ผ่าน winget) |
| Android SDK | `D:\android-sdk` (command-line tools เท่านั้น ไม่ใช่ Android Studio เต็ม) |
| Android SDK packages | platform-tools, emulator, platforms android-34/36, build-tools 34.0.0/28.0.3, NDK 28.2.13676358 (Gradle โหลดเอง) |
| AVD | `lifeplan_avd` — Android 14 (API 34), Pixel 6, google_apis x86_64 — เก็บที่ `D:\android-avd` |
| Cache (ย้ายมา D: เพราะ C: พื้นที่เหลือน้อย) | `GRADLE_USER_HOME=D:\gradle-home`, `PUB_CACHE=D:\pub-cache`, `ANDROID_AVD_HOME=D:\android-avd` |
| Playwright (สำหรับแคปภาพ/ทดสอบ) | ติดตั้งไว้ที่ scratchpad ของ session เดิม ถ้าต้องใช้ใหม่ให้ `npm install playwright` ใน scratchpad แล้ว `npx playwright install chromium` |

ยังไม่ได้ติดตั้ง: Xcode (ต้องใช้ Mac สำหรับ iOS), Visual Studio C++ workload (ต้องใช้ถ้าจะ build Windows desktop .exe — ไม่จำเป็นสำหรับ Android/iOS/Web)

---

## 3. โครงสร้างโปรเจกต์ (`lifeplan_app/lib/`)

```
lib/
  main.dart                     ← entry point, เช็ค session แล้วเข้า Login หรือ RootShell
  theme/                        ← สี, ฟอนต์ (Plus Jakarta Sans)
  models/                       ← data class ของแต่ละ entity (มี toMap/fromMap)
  data/
    hive_boxes.dart             ← ชื่อ Hive box ทั้งหมด + init()
    hive_repository.dart        ← generic CRUD wrapper
    repositories/                ← repository เฉพาะแต่ละโมดูล (ต่อยอด/เพิ่ม field ที่นี่)
    seed_data.dart               ← ข้อมูลตัวอย่างตอนติดตั้งครั้งแรก
    insights.dart                ← คำนวณ % ความคืบหน้าแต่ละด้านจากข้อมูลจริง
    repositories/goal_settings_repository.dart ← เป้าหมาย (ออม/ยอดขาย/ครั้งออกกำลังกาย) ที่ผู้ใช้แก้ได้ในหน้า GoalSettingsScreen
  screens/                      ← 1 ไฟล์ต่อ 1 หน้าจอ, ชื่อไฟล์ตรงกับหน้าจอ
    nutrition_screen.dart       ← ทานอาหาร & โภชนาการ (มื้ออาหาร, สารอาหาร, วิตามิน, น้ำดื่ม, มื้อรอบออกกำลังกาย)
    crm_screen.dart             ← ลูกค้า & ขายประกัน (รายชื่อลูกค้า + รายงานผลงานประจำสัปดาห์ P-A-S-R-F-N-T)
  widgets/                      ← component ใช้ร่วม (การ์ด, ปุ่ม, ฟอร์ม, progress bar)

test/
  repository_test.dart          ← unit test ของ Hive persistence (เชื่อถือได้, เร็ว)
  widget_test.dart              ← smoke test หน้า Login เท่านั้น (ดูข้อ 5 ว่าทำไมไม่มี test แบบ tap-through)
  nutrition_screen_test.dart    ← test หน้าโภชนาการ (box in-memory แบบเดียวกับ schedule_screen_test)
  dashboard_screen_test.dart    ← test การ์ดโภชนาการบนหน้าหลัก
  crm_screen_test.dart          ← test รายงานสัปดาห์ P-A-S-R-F-N-T (แสดงผล, เลื่อนสัปดาห์, คัดลอกส่งหัวหน้า)
```

**เพิ่มโมดูลใหม่**: เพิ่ม model → เพิ่ม box name ใน `hive_boxes.dart` → สร้าง repository ต่อยอดจาก `HiveRepository<T>` → ใช้ `ValueListenableBuilder`/`AnimatedBuilder(animation: Listenable.merge([...repo.listenable()]))` ในหน้าจอเพื่อ auto-rebuild เมื่อข้อมูลเปลี่ยน

---

## 4. ปัญหาที่เจอแล้วและวิธีแก้ (กันเสียเวลาแก้ซ้ำ)

1. **Android cmdline-tools URL ผิด** — `edgedl.me.gvt1.com` ที่ได้จาก WebFetch คืนค่า 404 ให้ใช้ `https://dl.google.com/android/repository/commandlinetools-win-<build>_latest.zip` แทน (เช็คด้วย `Invoke-WebRequest -UseBasicParsing` ก่อนเสมอ — ไม่มี `-UseBasicParsing` จะ error "NonInteractive mode" ทั้งที่ไม่เกี่ยวกับ network)

2. **sdkmanager --licenses ไม่ยอมรับครบ** — เพราะ pipe แบบ PowerShell native (`"y"*20 | & sdkmanager`) ไม่เสถียรกับโปรแกรมที่ wrap เป็น .bat ให้เขียน "y" หลายบรรทัดลงไฟล์แล้ว redirect ผ่าน `cmd /c "sdkmanager --licenses < yesfile.txt"` แทน (เชื่อถือได้กว่ามาก)

3. **avdmanager ไม่รับ `--sdk_root`** — flag นี้ใช้กับ sdkmanager เท่านั้น avdmanager ใช้ `ANDROID_SDK_ROOT`/`ANDROID_HOME` จาก environment แทน

4. **`flutter test` ค้าง ~90 วินาทีแล้ว fail แบบเงียบ** — สาเหตุจริง: `testWidgets` รันใน FakeAsync zone ซึ่ง**ไม่รอ real file I/O ของ Hive** (`box.put()` เป็น I/O จริงผ่าน dart:io) ทำให้ `pumpAndSettle()` คืนค่าทั้งที่ข้อมูลยังไม่บันทึกเสร็จ
   - วิธีแก้ที่ใช้จริง: แยก unit test ของ repository ไปใช้ `test()` ธรรมดา (ไม่ใช่ `testWidgets`) ซึ่งไม่มี FakeAsync — เชื่อถือได้และเร็วกว่ามาก (`test/repository_test.dart`)
   - เก็บ `testWidgets` ไว้แค่ smoke test ที่ไม่ต้องรอ async I/O จริง

5. **`GoogleFonts.config.allowRuntimeFetching = false` ทำให้ throw ใน test** — เพราะไม่ได้ bundle ไฟล์ฟอนต์ไว้ใน assets วิธีแก้: เพิ่ม `ThemeData? theme` parameter ให้ `LifePlanApp` แล้วส่ง `ThemeData(useMaterial3: true)` ธรรมดาตอนเทสต์แทน ไม่ต้องยุ่งกับ GoogleFonts เลย

6. **Gradle ดาวน์โหลดพัง (`504` จาก GitHub)** — `services.gradle.org` redirect ไปที่ `github.com/gradle/gradle-distributions/releases/...` ซึ่งบางครั้ง edge node ที่ใกล้ (สังเกตว่าเป็น region `southeastasia`) เกิด 504 ชั่วคราว **ลองใหม่อีกครั้งมักจะผ่าน** (เจอเองก็หายเองภายใน 1-2 นาที) ถ้ายังไม่หายให้ลอง curl เช็คตรง ๆ ก่อนเสียเวลา debug ฝั่ง Flutter

7. **flutter doctor บอกว่าต้องการ Android SDK 36 + build-tools 28.0.3** — นอกจาก platform-tools/platform-34/build-tools-34 แล้ว ต้องติดตั้ง `platforms;android-36` และ `build-tools;28.0.3` เพิ่มด้วย (Flutter เช็คทั้งสองช่วงเวอร์ชัน)

8. **`sdkmanager` กลายเป็น wrapper ของ Android CLI ตัวใหม่** (cmdline-tools 23.0 ขึ้นไป) — ชื่อแพ็กเกจแบบเดิม `platforms;android-34` จะขึ้น `Package platforms not found` เพราะถูกแยกที่ `;` ให้ใช้ `android.exe` ตรง ๆ และใช้ `/` แทน `;`:
   ```bash
   D:/android-sdk/cmdline-tools/latest/bin/android.exe --sdk='D:\android-sdk' sdk install platforms/android-34 build-tools/34.0.0 system-images/android-34/google_apis/x86_64
   D:/android-sdk/cmdline-tools/latest/bin/android.exe --sdk='D:\android-sdk' sdk list --all   # ดูชื่อแพ็กเกจ
   ```
   ไม่ต้องสั่ง `--licenses` แล้ว (ขึ้น warning ว่าไม่จำเป็น) ส่วน `avdmanager.bat create avd -k "system-images;android-34;google_apis;x86_64"` ยังใช้ชื่อแบบเดิมได้

9. **`flutter build apk` ครั้งแรก fail: `sdkmanager.bat finished with non-zero exit value -1073740791 (NTSTATUS 0xC0000409)`** — Gradle พยายามโหลด NDK (`flutter.ndkVersion` = 28.2.13676358) ผ่าน `sdkmanager.bat` ตัวใหม่แล้ว crash **สั่ง build ซ้ำอีกครั้งจะผ่าน** เพราะรอบถัดไป Gradle ใช้ตัวโหลดภายในของตัวเองติดตั้ง NDK ได้ (หรือติดตั้งล่วงหน้าด้วย `android.exe sdk install ndk/28.2.13676358`)

10. **"Building with plugins requires symlink support. Please enable Developer Mode"** — `flutter test` ผ่านได้โดยไม่ต้องเปิด แต่ `flutter build apk` จะหยุดทันที (โปรเจกต์มี plugin เช่น file_picker, flutter_local_notifications) ต้องเปิด Windows Developer Mode ก่อน: `start ms-settings:developers` แล้วเปิดสวิตช์ หรือสั่งผ่าน registry (ต้องกด UAC):
    ```powershell
    Start-Process reg.exe -ArgumentList 'add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /t REG_DWORD /f /v AllowDevelopmentWithoutDevLicense /d 1' -Verb RunAs -Wait
    ```
    ตรวจว่าเปิดจริงด้วย `reg query "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /v AllowDevelopmentWithoutDevLicense` (ต้องได้ `0x1`) — การเปิดหน้า Settings อย่างเดียวไม่ได้แปลว่าเปิดสวิตช์แล้ว

11. **Gradle แก้ชื่อ `repo.maven.apache.org` ไม่ได้ → build ล้มตอนโหลด dependency ใหม่** — อาการ: `Could not resolve org.jetbrains.kotlinx:kotlinx-coroutines-android` / "Got socket exception during request. It might be caused by SSL misconfiguration" สาเหตุจริงคือ **DNS ของเครือข่ายนี้แก้ชื่อ repo.maven.apache.org ไม่ได้** (`nslookup` ผ่าน 8.8.8.8 ได้ปกติ แต่ resolver ของเครื่องไม่ได้) วิธีแก้ที่ใช้: เพิ่ม mirror ของ Maven Central ที่ Google โฮสต์ (`https://maven-central.storage-download.googleapis.com/maven2/`) เป็น repository ตัวแรกใน `android/build.gradle.kts` และ `android/settings.gradle.kts` — ถ้าเจออาการนี้อีกให้เช็ค DNS ก่อน อย่าเพิ่งโทษ Gradle/SSL

12. **แจ้งเตือนไม่ขึ้น/ไม่มีเสียงเฉพาะบน release build (debug ปกติ)** — สาเหตุ: `isShrinkResources = true` ลบ `drawable/ic_notification` ทิ้ง เพราะไอคอนถูกอ้างชื่อจากฝั่ง Dart เท่านั้น Gradle จึงมองไม่เห็นการใช้งาน วิธีแก้: `android/app/src/main/res/raw/keep.xml` ที่ระบุ `tools:keep="@drawable/ic_notification,@raw/alarm_chime"` (เสียงเตือนโดนลบด้วยเหตุผลเดียวกัน) ตรวจว่าไอคอนรอดด้วย `aapt2 dump resources <apk> | grep ic_notification`

13. **`adb install -r` ขึ้น INSTALL_FAILED_VERSION_DOWNGRADE** — APK จาก `--split-per-abi` มี versionCode บวก 1000×ABI (x86_64 = 4001) ส่วน APK รวมเป็น 1 การสลับไปมาระหว่างสองแบบต้อง `adb uninstall` ก่อน

14. **`JAVA_HOME` ชนกับโปรเจกต์อื่น** — เครื่องนี้เดิมตั้ง `JAVA_HOME` เป็น GraalVM JDK 21 (`D:\POS\API\...`) ตอนนี้ user-level ถูกเปลี่ยนเป็น Temurin 17 แล้ว ถ้างานอื่นต้องใช้ JDK 21 ให้ export เฉพาะ session นั้น

15. **`flutter build apk` ล้มที่ `:android_file_picker:compileReleaseKotlin` — "Could not close incremental caches"** (stack trace มี `FilesKt.toRelativeString` / `RelocatableFileToPathConverter`) — สาเหตุ: Kotlin incremental compiler แปลง path เป็น relative ไม่ได้เมื่อซอร์สของ plugin (pub cache บน **C:**) กับโฟลเดอร์ build (**D:**) อยู่คนละไดรฟ์ `flutter clean` แล้ว build ใหม่ก็ยังล้มเหมือนเดิม วิธีแก้ที่ใช้: `kotlin.incremental=false` ใน `android/gradle.properties` (commit แล้ว) — build ผ่านใน ~2–4.5 นาที อีกทางคือตั้ง `PUB_CACHE` ให้อยู่ไดรฟ์เดียวกับโปรเจกต์

16. **widget test ที่กดบันทึก/ลบผ่านหน้าจอค้างไม่จบ** (ต่อจากข้อ 4) — `box.put()` บน Hive ที่เป็นไฟล์จริงไม่จบใน FakeAsync ของ `testWidgets` ฟอร์มจึงไม่ปิด และ `pumpAndSettle()` วนไม่จบ ส่วน `--timeout` ของ `flutter test` ก็ไม่ช่วยตัดให้ วิธีแก้ใน `test/schedule_screen_test.dart`: เปิด box แบบ in-memory `Hive.openBox<Map>(name, bytes: Uint8List(0))` แล้วใน `tearDown` ต้อง `await Hive.close()` ก่อน `tearDownTestHive()` (ไม่งั้นขึ้น "unsupported for memory boxes") ปุ่มที่อยู่ล่าง bottom sheet ต้อง `ensureVisible` ก่อน `tap` เพราะจอเทสต์สูงแค่ 600px — รันเทสต์ครอบด้วย `timeout 280 flutter test ...` กันค้าง

---

## 5. สิ่งที่ยังไม่ได้ทำ (เรียงตามลำดับที่ควรทำ)

- [ ] **Auth จริง** — โครงพร้อมแล้ว (2026-09-18) เหลือขั้นตอนที่ต้องใช้บัญชีภายนอก:
  - สมัคร Firebase, สร้างโปรเจกต์, เปิด Sign-in method (Email/Password + Google), วาง `android/app/google-services.json`, ลงทะเบียน SHA-1 ของ keystore ทั้ง debug และ release
  - เพิ่ม `firebase_core`/`firebase_auth` (+ `google_sign_in`, `sign_in_with_apple`) แล้วเขียน `FirebaseAuthService implements AuthService` — ดูขั้นตอนเต็มที่หัวไฟล์ `lib/data/auth/auth_service.dart`
  - ใช้ `uid` ของ Firebase เป็น `UserProfile.id` เพื่อให้ใช้เป็น `userId` ตอนทำ sync ข้อ 5 ได้เลย
- [x] **สำรอง/กู้คืนข้อมูลเป็นไฟล์ JSON** — ทำแล้ว (2026-09-15): `lib/data/backup.dart` + หน้า `BackupScreen` (โปรไฟล์ → สำรอง & กู้คืนข้อมูล)
  - ส่งออก: รวมทุก box เป็น JSON แล้วส่งผ่านแผงแชร์ของระบบ (share_plus) เลือกเก็บที่ไดรฟ์/แชทได้
  - กู้คืน: เลือกไฟล์ด้วย file_picker → ยืนยัน (โชว์จำนวนรายการในไฟล์) → เขียนทับทั้งหมด
  - ตรวจไฟล์ให้ครบก่อนเขียนจริง ไฟล์เสียจะไม่ทำให้ข้อมูลเดิมหายครึ่ง ๆ กลาง ๆ (มี test คุมที่ `test/backup_test.dart`)
  - ทดสอบจริงบน emulator แล้วทั้งส่งออกและกู้คืน
- [x] **ตั้งชื่อ/ไอคอน + keystore จริง + split APK** — ทำแล้ว (2026-09-15)
  - ชื่อแอปบนเครื่อง = "LifePlan", ไอคอนใหม่ (ดู `assets/icon/`, สร้างใหม่ด้วย `dart run flutter_launcher_icons`)
  - keystore จริงที่ `android/lifeplan-release.jks` + `android/key.properties` (**ไม่อยู่ใน git** — อ่าน `android/README-signing.md` ก่อน ห้ามทำหาย)
  - `--split-per-abi` ลดขนาดจาก 50.6MB เหลือ arm64 17.9MB
  - เปิด minify + shrinkResources ใน release build

- [x] **แจ้งเตือนตามตารางเวลา** — ทำแล้ว (2026-09-15): `lib/data/notifications.dart` + หน้า `NotificationSettingsScreen` (โปรไฟล์ → การแจ้งเตือน)
  - เตือนซ้ำทุกสัปดาห์ตาม weekday+เวลาของกิจกรรม, เลือกเตือนล่วงหน้าได้ (ตรงเวลา/5/10/15/30/60 นาที)
  - ตั้งใหม่ทั้งชุดทุกครั้งที่ตาราง/การตั้งค่าเปลี่ยน และตอนเปิดแอป (`main.dart`) — ไม่มีการเตือนค้างของกิจกรรมที่ลบแล้ว
  - ใช้ตารางแบบ inexact จึงไม่ต้องขอสิทธิ์ SCHEDULE_EXACT_ALARM (ไม่เด้งผู้ใช้ออกไปหน้าตั้งค่าระบบ)
  - `test/notification_schedule_test.dart` คุมการคำนวณเวลาเตือน (รวมเคสเตือนล่วงหน้าข้ามเที่ยงคืน)
  - เพิ่มเก็บค่าใน box `app_settings` (อยู่ในไฟล์สำรองด้วยแล้ว)

- [x] **ป๊อปอัป + เสียงแจ้งเตือนเมื่อถึงเวลา** — ทำแล้ว (2026-09-16)
  - **เสียง**: `android/app/src/main/res/raw/alarm_chime.wav` (ระฆัง 3 โน้ต A–C#–E สร้างด้วยสคริปต์ ไม่ได้ดึงจากที่อื่น) ตั้งเป็นเสียงของช่องแจ้งเตือน
  - เสียง/สั่นบน Android ผูกกับ *channel* และแก้ไม่ได้หลังสร้างช่องแล้ว — `NotificationService` จึงใช้ id ช่องต่างกันตามรูปแบบ (`schedule_reminders_s{0,1}_v{0,1}`) แล้วลบช่องที่ไม่ได้ใช้ทิ้งทุกครั้งที่ sync (รวมช่องเก่า `schedule_reminders`) ไม่งั้นปิดเสียงแล้วจะไม่มีผลจนกว่าจะถอนแอป
  - ต้องเพิ่ม `@raw/alarm_chime` ใน `res/raw/keep.xml` ด้วย ไม่งั้น `shrinkResources` ลบทิ้งแล้ว release build จะเงียบ (ปัญหาเดียวกับ `ic_notification` ข้อ 12) — ตรวจแล้วว่ารอด: `unzip -l app-release.apk | grep .wav`
  - **ป๊อปอัปกลางจอ**: `lib/widgets/reminder_popup.dart` — `ReminderHost` ครอบทั้งแอปผ่าน `MaterialApp.builder` + `navigatorKey` (ใช้ context ของตัวเองไม่ได้ เพราะ builder อยู่เหนือ Navigator)
  - `lib/data/reminder_watcher.dart` เป็นตัวจับเวลาฝั่งแอป (ตรวจทุก 20 วินาที) ให้ป๊อปอัปเด้ง *ตรงเวลา* ไม่ต้องรอ Android ปลุกแบบ inexact; หยุดทำงานเมื่อแอปอยู่เบื้องหลังหรือผู้ใช้ปิดการเตือน (จึงไม่มี Timer ค้างตอนรัน `flutter test`)
  - กันเด้งซ้ำด้วย `occurrenceKey` = `eventId@remindAt` — รอบสัปดาห์หน้าได้คีย์ใหม่จึงยังเด้งได้ปกติ
  - กดการแจ้งเตือนจากแถบสถานะ → เปิดป๊อปอัปของกิจกรรมนั้น (payload = `event.id`, รองรับทั้งตอนแอปเปิดค้างและตอนแอปถูกปลุกขึ้นมาใหม่)
  - ปุ่ม "เตือนอีกใน 5 นาที" ตั้งทั้ง Timer ในแอปและการแจ้งเตือนของระบบ (id 5000+) เผื่อผู้ใช้ปิดแอปไปก่อน
  - **เจตนา**: เสียงมาจากการแจ้งเตือนของระบบทางเดียว ป๊อปอัปสั่นอย่างเดียวไม่เล่นเสียงเอง — ไม่งั้นจะดังซ้อนกันสองที
  - test ใหม่: `test/reminder_watcher_test.dart`, `test/reminder_popup_test.dart` (รวมชุดทดสอบเป็น 60 tests)

- [ ] **Sync ข้ามอุปกรณ์** — Hive เก็บในเครื่องเท่านั้น ถ้าต้องการ sync ต้องมี backend (Firestore เข้ากับโครงสร้างที่ออกแบบไว้ใน `DATA_MODEL.md` ได้ทันที เพราะแต่ละ entity มี `userId` เป็น partition key อยู่แล้ว)
- [ ] **iOS build** — โค้ด Dart เดียวกันนี้พร้อมสำหรับ iOS แต่ต้องมี Mac + Xcode มา build/test จริง
- [x] **มุมมองตารางเวลารายสัปดาห์** — ทำแล้ว (2026-09-15): `ScheduleEvent` มีฟิลด์ `weekday` (1–7), `ScheduleRepository.getByWeekday()/getWeek()`, `ScheduleScreen` เป็น StatefulWidget สลับ "วัน / สัปดาห์" ได้ — มุมมองสัปดาห์เป็นตาราง 7 คอลัมน์เลื่อนแนวนอน, แถบ 7 วันกดเลือกวันได้และมีจุดบอกว่าวันไหนมีกิจกรรม, ฟอร์มเพิ่มกิจกรรมเลือกวันได้
  - เรคคอร์ดเก่าที่บันทึกก่อนหน้านี้ (ไม่มี `weekday`) จะถูกอ่านเป็นวันจันทร์ — มี test คุมไว้ที่ `repository_test.dart`
  - Dashboard "ตารางวันนี้" กรองเฉพาะ weekday ของวันนี้แล้ว (เดิมโชว์รวมทุกวัน)
  - test หน้าจอ: `test/schedule_screen_test.dart` (ใช้ Hive box แบบ in-memory เพื่อเลี่ยงปัญหา FakeAsync ข้อ 4/16)
- [x] **แก้ไข/ลบกิจกรรมในตารางเวลา** — ทำแล้ว (2026-09-15 ค่ำ) ดูข้อ 0
- [x] **โมดูลคุณภาพการนอน** — ทำแล้ว (2026-09-16) ดูข้อ 0
  - บันทึก/แก้ไข/ลบการนอนรายคืน, กราฟ 7 คืน, ค่าเฉลี่ย + ความสม่ำเสมอของเวลาเข้านอน, คำแนะนำจากข้อมูลจริง, เป้าเวลานอนตั้งเองได้
  - ต่อยอดได้: ~~เตือนถึงเวลาเข้านอนอัตโนมัติ~~, ~~กราฟย้อนหลังเป็นเดือน~~, ~~จับคู่คุณภาพการนอนกับคาเฟอีน/มื้อดึก~~ (ทำครบแล้ว 2026-09-18 ดูรอบด้านบน)
- [x] **โมดูลทานอาหาร & โภชนาการ** — ทำแล้ว (2026-09-16) ดูข้อ 0
  - บันทึก/แก้ไข/ลบมื้ออาหารพร้อมสารอาหารและวิตามิน, กดเพิ่ม-ลดน้ำดื่มทีละแก้ว, เป้าหมายโภชนาการตั้งเองได้
  - ต่อยอดได้: ~~กราฟย้อนหลังหลายวัน~~, ~~แจ้งเตือนมื้ออาหาร/ดื่มน้ำ~~, ~~ฐานข้อมูลเมนูอาหารไทย~~ (ทำครบแล้ว 2026-09-18 ดูรอบด้านบน) — เหลือ: เพิ่มช่องวันที่ในฟอร์มมื้ออาหารเพื่อบันทึกย้อนหลังได้
- [x] **แก้ไขรายการในหน้าอื่น** — ทำแล้ว (2026-09-17) ดูข้อ 0 — งานประจำ/ออกกำลังกาย/ลูกค้า/การเงิน/เรียนรู้ แก้ไขและลบได้ทุกหน้า
- [x] **ทดสอบแก้ไข/ลบบน emulator** — ทำแล้ว (2026-09-17) ติดตั้ง `app-x86_64-release.apk` บน `lifeplan_avd` แล้วลองจริง:
  - งานประจำ: เปลี่ยนสถานะเป็นกำลังทำ + 30% → ย้ายไปหมวด "กำลังดำเนินการ" ถูกต้อง
  - การเงิน: เปิดฟอร์มจากการแตะแถว, กล่องยืนยันลบขึ้นพร้อมจำนวนเงิน, กดยกเลิกแล้วรายการยังอยู่, บันทึกแล้ววันที่ยังเป็น "3 วันก่อน"
  - ตารางเวลา: ย้ายวันจันทร์ → พฤหัส แล้วเด้งไปวันพฤหัสทันที, ปัดซ้ายขึ้นกล่องยืนยัน, ย้ายกลับวันจันทร์ได้
  - **emulator เครื่องนี้**: เดิมตั้ง `hw.gpu.enabled=no` + RAM 2G ทำให้ "System UI isn't responding" ตลอด → แก้ `config.ini` เป็น GPU `swiftshader_indirect` + RAM 4096 (สำรองไฟล์เดิมไว้ที่ `config.ini.bak-20260917`) — `-gpu host` บน Intel UHD ทำ emulator ปิดตัวตอนบูต; หลังบูตอาจยังเจอ ANR หนึ่งครั้ง กด Wait แล้วใช้ได้
  - ตรวจหน้าจอด้วย `adb shell uiautomator dump` (Flutter ส่งข้อความผ่าน accessibility) ใน Git Bash ต้อง `export MSYS_NO_PATHCONV=1` ไม่งั้น `/sdcard/...` ถูกแปลงเป็น path Windows
- [x] **build APK ด้วย release key** — ทำแล้ว (2026-09-19) สร้าง keystore ใหม่บนเครื่องนี้ ดูรอบด้านบน + `android/README-signing.md`
- [x] **เป้าหมายที่ตั้งค่าได้เอง** — ทำแล้ว (2026-09-15): `GoalSettings` model + box `goal_settings` + หน้า `GoalSettingsScreen` (เข้าจาก โปรไฟล์ → ตั้งค่าเป้าหมาย) ทุกหน้าที่ใช้เป้าหมาย rebuild อัตโนมัติเมื่อบันทึก
  - การ์ดออกกำลังกายบน Dashboard ใช้ "เสร็จ / เป้าต่อสัปดาห์" ตรงกับ % แล้ว (2026-09-15)
- [ ] **Windows desktop build** — ต้องติดตั้ง Visual Studio "Desktop development with C++" workload ก่อน ถ้าต้องการ .exe (ไม่จำเป็นสำหรับเป้าหมายหลักคือ Android/iOS)

---

## 6. ลิงก์ / ไฟล์อ้างอิง

- ดีไซน์ (Claude Design canvas, แก้ไขได้): https://claude.ai/code/artifact/c58583c1-3ff8-4b8f-8907-45dc918aa714
- ภาพหน้าจอดีไซน์ + ภาพแอปที่รันจริง: `screenshots/` (ไฟล์ `flutter_*` = build web, `live_*` = คลิกทดสอบผ่าน Playwright, `emulator_*` = รันจริงบน Android emulator)
- โครงสร้างข้อมูล: `DATA_MODEL.md`
- ภาพรวมฟีเจอร์/โมดูล: `README.md`
- บันทึกรอบโมดูลโภชนาการ (16 ก.ย. 2026): `16092026 โภชนาการ.md`
