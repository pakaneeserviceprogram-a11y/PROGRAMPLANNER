# 16/09/2026 — โมดูลทานอาหาร & โภชนาการ (Nutrition)

บันทึกรอบงานวันที่ 16 ก.ย. 2026 บนเครื่อง `D:\WORK\PROGRAMPLANNER`
ใช้ไฟล์นี้อ่านต่อเมื่อไปทำงานต่อที่เครื่องอื่น (ภาพรวมโปรเจกต์อยู่ที่ `README.md`, วิธีทำงานต่ออยู่ที่ `HANDOFF.md`)

---

## 1. ทำอะไรไปบ้าง

เพิ่มโมดูลใหม่ทั้งชุด: **ทานอาหาร & โภชนาการ** เข้าจากการ์ดในหน้าหลัก

| สิ่งที่ผู้ใช้ทำได้ | ที่อยู่ในโค้ด |
|---|---|
| บันทึก/แก้ไข/ลบมื้ออาหาร พร้อมเวลา (มื้อเช้า/กลางวัน/เย็น/ของว่าง) | `lib/screens/nutrition_screen.dart` |
| กรอกคุณค่าต่อมื้อ: พลังงาน (kcal), โปรตีน, แป้ง, ไขมัน, น้ำตาล, วิตามิน A–K | `lib/models/meal_entry.dart` |
| ระบุว่าเป็นมื้อ "ก่อนออกกำลังกาย" / "หลังออกกำลังกาย" | `WorkoutTiming` ใน `meal_entry.dart` |
| นับน้ำดื่มรายวัน กดเพิ่ม/ลดทีละแก้ว (แก้วละ 250 มล.) | `lib/models/water_log.dart` + `water_repository.dart` |
| ตั้งเป้าโภชนาการต่อวันเอง (พลังงาน/โปรตีน/แป้ง/ไขมัน/เพดานน้ำตาล/น้ำ) | `lib/screens/goal_settings_screen.dart` |
| เห็นสรุปวันนี้ + เตือนเมื่อน้ำตาลเกินเพดาน + บอกวิตามินที่ยังขาด | `nutrition_screen.dart` |
| เทียบพลังงานที่กินกับที่เผาผลาญ + ดูเวลาออกกำลังกายของวันนี้จากตารางเวลา | การ์ด "โภชนาการสำหรับออกกำลังกาย" |

### ไฟล์ใหม่
```
lib/models/meal_entry.dart                  ← MealEntry, MealType, Vitamin, WorkoutTiming, NutritionTotals
lib/models/water_log.dart                   ← WaterLog (วันละ 1 รายการ, คีย์ = yyyy-MM-dd)
lib/data/repositories/meal_repository.dart  ← getForDay / getToday / totalsForDay
lib/data/repositories/water_repository.dart ← getForDay / addGlass / removeGlass
lib/screens/nutrition_screen.dart           ← หน้าจอโมดูล
test/nutrition_screen_test.dart             ← 3 เคส (แสดงมื้อ, กดเพิ่มน้ำ, สถานะว่าง)
test/dashboard_screen_test.dart             ← การ์ดโภชนาการบนหน้าหลัก
```

### ไฟล์เดิมที่แก้
- `hive_boxes.dart` — box ใหม่ `meal_entries`, `water_logs`
- `backup.dart` — เพิ่ม 2 box เข้าไฟล์สำรอง (**`backupFormatVersion` ยังเป็น 1** เพราะไฟล์สำรองเก่ายังอ่านได้เหมือนเดิม box ที่ขาดจะถูกข้าม)
- `life_category.dart` + `app_colors.dart` — หมวดใหม่ `nutrition` (สีชมพูแดง `#D9506B`)
- `goal_settings.dart` — 6 ฟิลด์เป้าโภชนาการ (ค่าเริ่มต้น 2,000 kcal / 60 / 250 / 65 / 25 ก. / 2,000 มล.)
- `insights.dart` — `Insights.nutritionScore()` เข้าคะแนนรวมทุกด้าน
- `dashboard_screen.dart` — การ์ดโภชนาการเต็มแถว (แยก `_WideModuleCard` ใช้ร่วมกับการ์ดเรียนรู้)
- `progress_screen.dart` — เพิ่ม badge "มื้อวันนี้" + rebuild ตาม box ใหม่
- `seed_data.dart` — มื้อตัวอย่าง 3 มื้อ, น้ำ 4 แก้ว, กิจกรรมมื้ออาหารในตารางเวลา 3 รายการ

---

## 2. ของเดิมพังไหม

ไม่พัง — ข้อมูลที่บันทึกไว้ก่อนหน้านี้ใช้ต่อได้ทั้งหมด

- เรคคอร์ด `GoalSettings` เก่าที่ยังไม่มีฟิลด์โภชนาการ → `fromMap` เติมค่าเริ่มต้นให้อัตโนมัติ
- box ใหม่ว่างอยู่ → `seedIfEmpty()` จะใส่ข้อมูลตัวอย่างให้เฉพาะ box ที่ยังว่าง (โมดูลอื่นไม่ถูกแตะ)
- ไฟล์สำรองเวอร์ชันเก่า (v1) ยัง import ได้ปกติ

---

## 3. ยังไม่ได้ทำ / ทำต่อได้เลย

- [ ] **ทดสอบบน emulator หรือมือถือจริง** — รอบนี้มีแค่ `flutter analyze` + `flutter test` (37 tests ผ่านหมด) ยังไม่ได้กดใช้งานจริง
- [ ] **ไฟล์ดีไซน์ `.dc.html` ของหน้าโภชนาการ** — ยังไม่มี (หน้าอื่นมีครบ 10 ไฟล์) หน้านี้เขียนจากระบบสี/การ์ดชุดเดิมโดยตรง
- [ ] **กราฟย้อนหลังหลายวัน** — ข้อมูลเก็บแยกตามวันอยู่แล้ว (`MealEntry.date`, `WaterLog` คีย์รายวัน) แต่หน้าจอโฟกัสเฉพาะ "วันนี้"
- [ ] **ฐานข้อมูลเมนูอาหารไทยสำเร็จรูป** — ตอนนี้ต้องกรอกกรัม/แคลอรี่เอง
- [ ] **แจ้งเตือนมื้ออาหาร/ดื่มน้ำแยกต่างหาก** — ตอนนี้ยืมการแจ้งเตือนของตารางเวลา (สร้างกิจกรรมหมวดโภชนาการแล้วตั้งเวลา)
- [ ] **build APK รอบใหม่** — APK ที่มีอยู่ยังไม่มีโมดูลนี้

---

## 4. ไปทำต่อที่เครื่องอื่น

```bash
# เครื่องที่เคย clone แล้ว
cd <โฟลเดอร์โปรเจกต์>
git pull

# เครื่องใหม่
git clone https://github.com/pakaneeserviceprogram-a11y/PROGRAMPLANNER.git
cd PROGRAMPLANNER/lifeplan_app
flutter pub get
flutter test          # ต้องผ่าน 37 tests
flutter run -d chrome # เช็ค UI เร็วสุด
```

**อย่าลืม**: `android/lifeplan-release.jks` และ `android/key.properties` **ไม่อยู่ใน git** (ดู `android/README-signing.md`) ถ้าจะ build APK แบบ release ต้องเอา keystore จากเครื่องเดิมมาก่อน ไม่งั้นจะได้ไฟล์ที่เซ็นด้วย debug key ซึ่งติดตั้งทับของเดิมไม่ได้

ปัญหาสภาพแวดล้อมที่เคยเจอ (Gradle, pub cache คนละไดรฟ์, FakeAsync ใน widget test) รวมอยู่ใน `HANDOFF.md` ข้อ 4 แล้ว
